# BS-RoFormer-SW Model Notice

This is a Veloura Lucent project notice. It records the model source and the
license information available from that source; it is not an upstream license
file and does not grant additional rights.

## Model acquired by the app

- Model: `BS-RoFormer-SW`
- Source repository: `MrSimmo/BS_Roformer_SW-MLX`
- Fixed revision: `13edef2e713151522e4049e92f011e0543c45d53`
- Model weights: `bs_roformer_sw.safetensors`
- Model configuration: `bs_roformer_sw_config.json`
- License metadata at the source: `unknown`
- Upstream license statement at the source: `undeclared`

The source model card identifies the converted checkpoint as
`enerjazzer/BS-ROFO-SW-Fixed` / `BS-Rofo-SW-Fixed.ckpt`. The fixed MLX model is
an FP16 `safetensors` conversion of that checkpoint.

## Distribution behavior

Veloura Lucent does not bundle these two model files in the application or DMG.
The app shows the model source, fixed revision, license status, file list,
sizes, SHA-256 values, download URLs, and destination before the user can start
the download. The files are downloaded only after that explicit confirmation
and are validated before activation.

## Scope

The upstream license is not declared by the source repository. This notice
preserves that status rather than treating the model as MIT or otherwise
licensed. It is a technical provenance record, not legal advice.

Source status verified: 2026-08-11.
