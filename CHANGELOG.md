# チェンジログ

Veloura Lucentで利用者に影響する大きな変更と修正を記録します。
内部整理、テストだけの変更、細かな見た目の調整は省略します。

## 未リリース

### 変更

- 「Veloura Lucentについて」に、アプリのバージョン番号とビルド番号を表示しました。
- 開発中のプロジェクト版はビルドごとに新しいビルド番号を付け、配布版はGitのコミット数をビルド番号として使うようにしました。
- 比較動画で、タイトルと2つの役割の文字色・フォントサイズ・位置、背景色・背景画像を調整できるようにしました。音量・ピーク・強弱・ステレオ幅と音源の基本情報は、位置に加えて、一般的な5つの比率またはカスタム比率とサイズを変更できます。比較動画ウインドウは、Veloura Lucentを操作中の間だけ、ほかの通常ウインドウより前面に保ちます。左側の設定は読みやすい角丸枠で項目ごとに整理しました。
- 比較動画の音声と映像へ、フェードイン・フェードアウトを別々に設定できるようにしました。音に合わせて動くイコライザー・ドットは、標準の7色グラデーションと任意の3色グラデーションを切り替え、横幅、高さ、全体の拡大率、位置も調整できます。

### 修正

- 比較動画の音源情報は、右下の自由サイズ変更を廃止し、比率選択と1本のサイズスライダーへ変更しました。タイトル、役割、音源情報で同じドラッグ処理を使い、音源情報は1つの描画面として移動します。大きさを変える時は、枠、文字、間隔、余白、角丸をまとめて拡大・縮小し、既定の4分の1まで小さくできます。再生時間の更新と完成画面の更新を分け、再生中の不要な再描画を減らしました。比較動画ウインドウはVeloura Lucentが操作中の時だけ前面に表示します。再生操作を表示してもタイトルや役割が暗くならないようにし、左側の文字・数値入力欄は、ほかの設定と調和する見た目にそろえました。

## 2026-08-28

### 追加

- 比較動画作成機能をワークスペースへ追加しました。選んだ2つの音源を交互に再生し、比較用の動画として書き出せます。

### 修正

- 完了通知を押した時に別のVeloura Lucentを起動せず、処理状態を持つ既存の作業ウインドウへ戻るようにしました。
- 開発用のプロジェクト版と配布版を別のアプリとして識別し、同名アプリの取り違えを防ぐようにしました。
- 開発用ビルドが`/Applications`の配布版を停止または置き換えないようにしました。

## 2026-08-27

### 変更

- DMGインストーラー内のアプリとApplicationsリンクの配置を整え、初回起動の手順を分かりやすくしました。
