# Veloura Lucent の概要

## これは何をするアプリか
- 音声ファイルを読み込みます。
- まず、耳につくザラつきやシュワシュワ感を減らします。
- ノイズ除去は `弱い / 標準 / 強い` から選べます。
- そのあと、高い音の不足を少し補います。
- 高い音の補い方では、倍音寄りの成分と立ち上がりを見て、16kHz 以上の伸びを作ります。
- 高い音をどれだけ足すかは、軽い `Neural foldover` 推定器が自動で決めます。
- ノイズ除去では、高域の細かいザラつきも少し強めに抑えます。
- そのあと、別機能のマスタリングで仕上がりを整えます。
- 補正後と最終版の書き出しは、入力が `mp3` や `flac` でも `wav` で保存します。
- 補正段では最終音量を強く追い込まず、ピークが危ない時だけ安全側へ抑えます。
- マスタリングでは、低域、中低域、プレゼンス帯域、エアー帯域、ハーシュネスをまとめて整えられます。
- マスタリングでは、低域は広げすぎず、中高域だけ広げるようにして、芯が痩せにくい形にしています。
- マルチバンド圧縮は、低域・中域・高域を整理しつつ、超高域は潰しすぎないように分けています。
- さらに、入力・補正後・最終版の差を、1つの比較エリアで見比べられます。
- 上段では、左のレーダーで全体の方向、右の周波数カーブで帯域の変化を確認できます。
- 下段では、左の主な数値、右の帯域棒グラフで `入力 / 補正後 / 最終版` を比べられます。
- 用語は正式名称へ寄せて、`?` ボタンから読み方と意味を確認できます。
- ブラウザではなく、Mac の普通のアプリとして動きます。

## 技術アーキテクチャの概要
- 画面: `SwiftUI`
- 音声ファイルの読み書き: `AVFoundation`
- 周波数ごとの計算: `Accelerate`
- 実行入口: `script/build_and_run.sh`

図にすると、こうです。

```text
Macアプリ(SwiftUI)
  -> AudioProcessingService
    -> NativeAudioProcessor
      -> AudioFileService
      -> SpectralDSP
      -> 倍音/シマー解析
      -> Neural foldover推定
      -> ノイズ除去
      -> foldover系の高域補完
      -> 帯域抑制
    -> MasteringService
      -> MasteringAnalysisService
      -> MasteringProcessor
      -> de-esser
      -> 4バンドEQ
      -> 仕上げ
```

## コードベースの構造
- `Sources/VelouraLucent/App/`
  - アプリの起動です。Dock アイコンの上書きもここで行います。
- `Sources/VelouraLucent/Views/`
  - 画面です。差分サマリー、スペクトログラム、比較表示もここです。
- `Sources/VelouraLucent/Models/`
  - 画面の状態、ノイズ除去の強さ、マスタリング設定です。
- `Sources/VelouraLucent/Services/`
  - 補正処理、倍音解析、Neural foldover 推定、ノイズ除去、マスタリング処理、比較用メトリクス計算、音声ファイルの読み書き、ファイル選択などです。
- `Sources/VelouraLucent/Support/`
  - FFT まわりなどの共通処理です。
- `Tests/VelouraLucentTests/`
  - 動作確認用のテストです。
- `Resources/`
  - アプリアイコンの元画像です。`AppIcon-1024.png` を置きます。
- `Sources/VelouraLucent/Resources/`
  - Xcode から直接起動した時にも使える、実行時用の画像リソースです。
- `script/build_and_run.sh`
  - ビルドしてアプリを起動するスクリプトです。アイコン画像から macOS 用の Asset Catalog を組み立てて、`Assets.car` とアイコン情報を作ります。

## その技術を選んだ理由
- `SwiftUI`
  - Mac アプリの見た目を素直に組みやすいからです。
- `AVFoundation`
  - macOS 標準の音声読み書きが使えるからです。
- `Accelerate`
  - 周波数処理を Swift のまま実装しやすいからです。
- 段階式の自動処理
  - 非エンジニアでも、少ない選択で仕上がりを変えやすいからです。

## よくあるバグと修正方法
- 音声ファイルを開けない
  - 壊れたファイルか、対応外の形式の可能性があります。
- 出力ファイルができない
  - 元ファイルの場所に書き込み権限があるか確認します。
- `mp3` や `flac` 入力で書き出しに失敗する
  - いまは内部出力を `wav` に固定して避けています。保存名の拡張子が `wav` になっているか確認します。
- マスタリングが押せない
  - 先に補正を実行して、`*_lifter` を作ります。
- ノイズ除去を強くしすぎると音が細く聞こえる
  - まず `標準` で試して、必要な時だけ `強い` に上げます。
- 高域補完が強く聞こえる
  - 入力によっては倍音が少ないため、補完の効き方が変わります。
- 高域のザラつきが気になる
  - ノイズ除去の強さを上げると、高域の粒子感をより抑えやすいです。
- 高域が痛い
  - 詳細設定の `ハーシュネス抑制` を少し上げます。
- マスタリング後に広がるのに芯が弱い
  - `ステレオ幅` を下げます。いまは低域を広げない設計ですが、中高域は広がります。
- グラフが分かりにくい
  - まず上段で全体像を見て、次に下段で数値と帯域棒を確認すると分かりやすいです。
- 処理が重い
  - 長い音声や高負荷の環境では時間がかかります。

## 落とし穴と回避方法
- 処理は完全な AI モデルではありません
  - 高域補完は foldover を含む簡潔なヒューリスティック処理が中心で、補完量だけを軽い推定器で調整します。
- 補正は解析結果に応じて変わります
  - 倍音量、シマー、立ち上がり量、ノイズ量で効き方が少し変わります。
- マスタリングは別工程です
  - 補正と仕上げを分けて考えるほうが確認しやすいです。
- マスタリングの数値は厳密な配信規格測定ではありません
  - ラウドネスとトゥルーピークは近似計算ですので、最終納品前は別計測もあると安心です。
- Crest と LRA が下がりすぎると平坦になります
  - 比較表で `Crest` と `LRA` を見て、補正後から最終版で落ちすぎていないか確認します。
- 刺さり対策は高域全体の明るさと別です
  - `エアー帯域` を上げすぎたら、必要に応じて `ハーシュネス抑制` も調整します。
- 入力ごとに効き方が変わります
  - いつも同じ結果にはなりません。
- 出力形式は入力そのままではありません
  - `mp3` や `flac` を読んでも、補正後と最終版は `wav` になります。
- 長尺ファイルは重くなります
  - まず短い音で効き方を確認すると安全です。

## ベストプラクティス
- まずは `./script/build_and_run.sh` で起動します。
- アイコンを変える時は `Resources/AppIcon-1024.png` を差し替えます。
- Xcode から直接起動した時の Dock アイコンも、起動時に同じ画像へ上書きします。
- `swift build` と `swift test` を通してから変更を重ねます。
- 音声処理は「補正後」と「最終版」を分けて聞いて確認します。
- `ノイズ除去` は `標準` を基準にして、必要な時だけ変えます。
- マスタリング詳細設定は、まず `ハーシュネス抑制` と `中低域` から触ると分かりやすいです。
- 差分を見る時は、入力・補正後・最終版の3つを同じ場所で見比べると理解しやすいです。
- 無理に複雑な仕組みにせず、今の処理順を保って改善します。
