# Veloura Lucent の概要

## これは何をするアプリか
- 音声ファイルを読み込みます。
- 音声ファイルは、上部ツールバーの `音声を選ぶ` から選ぶほか、中央の作業画面へドラッグアンドドロップして読み込めます。
- まず、耳につくザラつきやシュワシュワ感を減らします。
- ノイズ除去は `弱い / 標準 / 強い` から選べます。
- そのあと、高い音の不足を少し補います。
- 高い音の補い方では、倍音寄りの成分と立ち上がりを見て、16kHz 以上の伸びを作ります。
- 高い音をどれだけ足すかは、軽い `foldover補完量推定` が自動で決めます。
- `foldover補完量推定` は、高域不足、16kHz以上の残り具合、18kHz以上の荒れも見て、足しすぎを避けます。
- ノイズ除去では、低中域の音楽成分があり、静かな区間より高域が自然に伸びている時は、8kHz〜20kHzの煌びやかさ、空気感、超高域をできるだけ残します。
- ノイズ除去では、静かな区間に出るヒスや、音楽成分と一緒に動いていない高域ノイズは下げます。高域保護は基本のノイズゲートにだけ使い、チリつき確認や最終mask全体を無条件に持ち上げません。
- ノイズ除去では、完全な静音部は今まで通り下げます。ただし、静音部より少し強く、演奏の余韻として残っているフレームでは、60Hz〜150Hzと150Hz〜250Hzを少し守り、低域と低中域が吸い込まれすぎないようにします。
- ノイズ除去では、10kHz〜16kHzの一瞬だけ出るチラつきも見て、AI生成音源のシュワシュワ感を抑えます。
- サ行とシマーの保護では、8kHz以上を常に下げず、短時間だけ大きく出るサ行、刺さり、チリつきだけを下げます。
- サ行保護後に5kHz〜9kHzの通常の明るさが下がりすぎ、サ行だけが相対的に目立つ時は、短時間ピーク以外の通常高域を戻し、まだ強い短時間ピークだけを軽く抑えます。
- そのあと、別機能のマスタリングで仕上がりを整えます。
- 補正後と最終版は、用途に合わせて `高品質保存`、`配信・納品用`、`CD用`、`試聴共有用` から選んで書き出せます。
- Stem Modeの44.1kHz成果物を`高品質保存`する時は、通常モードと共通のSRCで48kHz / 32-bit Floatへ変換します。有限な±1超過sampleは正規化・limiter・整数clampで変えずに保持し、変換内部errorはユーザー向けの書き出しerrorとして返します。
- 内部のプレビュー用ファイルは、音の情報を保つため `32-bit float WAV / 48kHz` のままです。
- 48kHz以外の音声を読み込む時は、OS標準の音声変換で48kHzにしてから処理します。
- `配信・納品用` は `24-bit PCM WAV / 48kHz`、`CD用` は `16-bit PCM WAV / 44.1kHz + TPDFディザ`、`試聴共有用` は `AAC .m4a / 48kHz / 256kbps` です。
- 補正段では最終音量を作らず、ノイズ除去、低域整理、最低限の高域修復、ピーク保護だけを行います。
- 補正段では、ハム、低域ゴロゴロ、サ行、シマー、ヒスを順番に見て、強い設定ほど実測で下がるようにしています。
- 補正段の低域ゴロゴロ整理では、静かな区間の20Hz〜150Hzは今まで通り下げます。ただし、標準補正の演奏中と余韻では60Hz〜150Hzの削り量だけを少し弱め、低域の土台が細くなりすぎないようにします。20Hz〜60Hzはハムやゴロゴロ感が戻りやすいため大きく戻しません。強い補正では、ハムと低域ゴロゴロをしっかり下げる目的を優先します。
- 補正段の高域保持では、8kHz〜16kHz の実際の音量が原音より少し痩せた時に、ヒス、シマー、サ行、16kHz以上の超高域が戻りすぎない範囲で少し戻します。
- 通常モードの補正段では、既存補正後・ピーク保護前に20Hz〜180Hzの左右位相も確認します。入力から存在する場合と補正後に生じた場合の両方を見分け、モノラル加算で明確に相殺する時間・周波数区画だけを左右同位相へ近づけます。判定に使う一時モノラル信号は保存せず、通常のL+Rモノラル加算を確認基準にします。低域の実在、モノラル相殺の改善、ステレオエネルギー上限を満たさない場合は変更前を維持します。モノラル入力、2チャンネル以外、問題のない低域、180Hzより上だけの逆相は変更しません。この工程はStem ModeのStem別補正と再ミックスには追加しません。
- 通常モードの低域位相修復を採用した時は、詳細ログへ、入力／既存補正後／位相修復後の内部相殺指標とモノラル低域損失、対象セル数、対象時間、原因、改善量、安全検証結果を表示します。内部相殺指標は音質点数ではなく、左右が打ち消し合う強さを同じ計算で比較する表示用指標です。表示用集計は既存STFT解析中に行い、検出条件、修復量、安全判定、音声出力へ接続しません。
- マスタリングでは、低域、中低域、プレゼンス帯域、エアー帯域、ハーシュネスをまとめて整えられます。
- 標準マスタリングでは、音量を前に出しすぎず、8kHz〜12kHz のきらめきをサ行の瞬間成分から分けて少し戻します。
- 標準マスタリングでは、低域と部屋鳴りが戻りすぎないよう、低域と中低域の持ち上げを控えめにしています。
- マスタリングのノイズ戻りガードでは、静かなヒスと短時間の高域チラつきを分けて見ます。持続する8kHz〜16kHzの音楽的な高域は、まとめて暗くしないように守ります。
- マスタリングでは、高域戻りガードを通常時は使わず、刺さりと高域戻りが同時に強い時だけ使います。
- マスタリングでは、ノイズを減らし続けるよりも、原音の透明感と空気感を残すことを優先します。
- マスタリングでは、最後のヒス・サ行・高域チラつき確認も緊急用に寄せ、少しの戻りで高域全体を削りすぎないようにしています。
- マスタリングでは、`targetLoudness` を必ず合わせる数値ではなく、仕上がりの音量目安として使います。安全に届く時は目標音量へ近づけ、音量変更は `自然`、`聴きやすく整える`、`押し出し強め`、`安全AI配信`、`YouTube / Spotify向け`、`リリース音圧重視` ごとの上限内に収めます。
- マスタリングでは、ノイズ戻りガードや高域保持のあとに最終音量を再確認します。目標音量との差だけで判断せず、True Peakの余裕、プリセットごとの音量変更上限、プリセットごとの目安超過幅、ノイズ戻りを守れる時は、最大2.0dBまで最終音量を戻します。目安超過幅は、自然と安全AI配信が0.75dB、聴きやすく整えるが1.0dB、YouTube / Spotify向けが1.25dB、押し出し強めが1.5dB、リリース音圧重視が2.0dBです。音量復帰後の全体確認で、補正後よりヒスや高域チラつきが種類ごとの上限を超えて戻った場合は、緊急上限ガードで抑え直します。ヒスは復帰前も確認し、復帰前が上限内なら音量復帰前へ戻します。補正後で高域が大きく失われ、原音参照で高域を戻す素材でも、補正後からのヒスや高域チラつきの戻り上限は守ります。復帰前も上限を超える場合は緊急上限ガードを続けます。
- マスタリングでは、8kHz〜12kHz の煌びやかさと 12kHz〜16kHz の空気感を、軽い帯域測定で確認し、最終版だけ暗くなりすぎないようにしています。
- マスタリングでは、補正後にサ行が強く残った素材だけ、最後に 5kHz〜9kHz の短時間ピークを軽く抑え、原音からの増えすぎを防ぎます。
- マスタリングログでは、ラウドネス、高域戻りガード、ノイズ戻りガードを分けて表示し、どこに時間がかかったか見やすくしています。
- マスタリングでは、低域は広げすぎず、中高域だけ広げるようにして、芯が痩せにくい形にしています。
- マスタリングでは、ラウドネス調整後に演奏中と余韻の20Hz〜1kHzだけを確認し、低域と低中域が下がりすぎた時だけ上限付きで少し戻します。60Hz〜150Hzは低域の土台として分けて見ます。150Hz〜500Hzは演奏中フレームだけ最大0.5dB程度まで軽く戻し、胴鳴りと厚みが薄くなりすぎないようにします。この150Hz〜500Hzの確認は補正後の音を基準にします。原音がある時も、150Hz〜500Hz以外の低域保護で原音を参考にし、完全に戻すのではなく削れすぎた分だけ自然に軽く戻します。さらに、最終ノイズ確認後にも、補正後より150Hz〜500Hzが下がっている時だけ、演奏中の150Hz〜500Hzを最大0.5dB戻します。最後にプリセットごとの音量上限を確認します。静かな区間のノイズ除去は弱めません。
- マルチバンド圧縮は、低域・中域・高域を整理しつつ、超高域は潰しすぎないように分けています。
- 詳細設定では `ダイナミクス保持` と `仕上げの強さ` で、平坦さと押し出し感をまとめて調整できます。
- 右設定パネルの補正の基本、掃除と修復、上級、マスタリングの基本、マスタリングの音色、マスタリングの上級は、提供されたノブ画像を使ったロータリーノブで調整します。ノブは値に合わせて回り、青い点の位置で現在値を見られます。
- 入力・補正後・最終版の差は、中央の作業画面で続けて確認できます。
- 中央の作業画面では、3つの音源を横長の波形で縦に並べます。波形は正方向／負方向のピークとRMSを縦方向の区間として描き、同じ時間目盛りとカーソル位置で比較します。各段は見やすい96 ptを初期高さとし、下端の細いハンドルを上下へドラッグすると68〜192 ptの範囲で個別に高さを変えられます。波形をクリックすると3つの音源の再生位置を同じ時間へ動かし、拡大中にドラッグすると3段共通の表示範囲を左右へ動かせます。時間目盛りの右下にあるスライダーと縮小／拡大ボタンは現在の再生位置を中心に連続的に表示範囲を変え、3段とも同じ範囲を表示します。先頭では右方向、途中では左右、末尾では左方向へ表示範囲が広がります。最大倍率は、保持している波形データと実際の表示幅から決まり、データを表示幅より粗く引き伸ばしません。拡大中の再生では、先頭と末尾を除いて再生位置を画面内へ保ちながら波形を連続して動かします。
- 中央の作業画面では、再生中スペクトルを1つのグラフに表示します。選択中のA/B比較に含まれる2音源だけを、同じ再生位置で並べて、周波数ごとの強さの違いを見ます。
- 再生中スペクトルの近くには `ベクトルスコープ` を表示します。いま再生している1音源だけを対象に、`Polar Sample`、`Polar Level`、`Lissajous` を標準のセグメント切り替えで選び、左右チャンネルの広がり、向き、位相ずれを確認します。停止中は軌跡を残さず、枠と説明だけを表示します。
- `Polar Sample` では半円型の表示に、サンプルごとの点、45度安全ライン、3秒で薄く消える履歴を表示します。クリップした点は赤く表示します。`Polar Level` では平均化した線を表示し、線の角度でステレオ位置、線の長さで振幅を見ます。線の履歴は3秒で薄くなりながら中心へ縮みます。右側には相関メーター、下側にはL/Rバランスメーターを表示します。
- ベクトルスコープの右側には `ラウドネスメーター` を表示します。再生中の音から Momentary LUFS、Short-Term LUFS、Integrated LUFS、True Peak dBTP を測り、目標LUFSとTrue Peak上限も同じ場所で確認します。単なる数値だけではなく、縦型メーターで現在値、目標線、上限線を見ます。
- 中央の作業画面では、スペクトログラムを横長の3段で表示します。時間ごとの高域、低域、ノイズの出方を同じ画面で見ます。色は3音源で共通の `0〜-100表示dB` を使い、右側の凡例で強さを確認できます。
- 品質チェックは右設定パネルにまとめています。音量低下、ピーク超過、高域の増えすぎ、ステレオ幅の急変、音の起伏の変化を知らせます。
- 右設定パネルでは、確認する音源を `入力 / 補正後 / 最終版` から選び、ラウドネス、True Peak、ダイナミクス、ステレオ幅を見ます。
- ノイズチェックは1つのカードにまとめ、入力、補正後、最終版のノイズ量を横棒で見せます。各行には「原音より低い」「少し戻りあり」などの判定も出します。
- ノイズチェックの横棒は実測値そのものではなく、見た目で差が大げさになりすぎない表示用スケールで出します。右側の数値と差分は実測値のままです。
- ヒスと高域のチラつきの横棒は、警告しきい値まで同じ目盛りに入れています。悪い値が全部同じ最大表示に見えないようにするためです。
- 未測定のノイズ行は、同じ長さの薄いグレーの棒で表示し、まだ測っていないことが分かるようにします。
- ノイズチェックのおすすめ設定は固定幅ではなく、補正で減った量、マスタリングで戻った量、残っているノイズ量から計算します。すでに上限や下限で動かせない項目は表示しません。
- 左サイドバーは `音源` と `工程` の2つに分けています。`音源` では入力、補正後、最終版それぞれのファイル名、保存場所、形式、サンプルレート、チャンネル、長さを同じ行の中で確認できます。`工程` では補正とマスタリングそれぞれの工程を確認できます。
- 左サイドバーの `工程` では、補正とマスタリングを別々に表示します。補正は17工程、マスタリングは19工程を上から順番に出し、完了、実行中、省略、失敗、待機をアイコンと文字で見分けられるようにしています。
- ファイル情報は実際の音声ファイルから取得します。WAVは取得できたビット深度を表示し、MP3やM4Aは存在しないビット深度を作らず、MP3またはAACと表示します。
- 固定下部では、左に直近の処理内容を最大4件、右に入力解析、補正処理、マスタリング、書き出しの全体工程を表示します。
- 直近の処理内容には、時刻、処理名、対象ファイル、取得できた解析値、処理中の進捗率を、内容に応じて表示します。完全な処理ログは別に残します。
- 全体工程は、補正またはマスタリングの実際の進捗率と連動します。待機中、処理中、完了の位置を4段階で確認できます。
- 詳細ログボタンを押すと、補正ログとマスタリングログの全文を同じ幅の2列で確認できます。長い行は折り返し、縦にスクロールできます。
- 解析表示で測ったLUFSは、プレビューの音量合わせ再生にも使います。プレビュー側で同じラウドネスをもう一度重く測らないためです。
- 解析表示でプレビュー帯域とスペクトログラムを同時に作る時は、STFT全体を大きな配列として持たず、1フレームずつ帯域値と表示セルを集計します。同じ音声から同じ周波数解析を2回作らず、表示用メモリも減らすためです。
- 解析表示は、比較、スペクトログラム、プレビュー、ノイズ確認、補正解析、マスタリング解析を別々の完了状態で扱います。重いノイズ確認を待たずに、先に終わった表示から画面へ出すためです。
- 解析表示の時間計測と、重い表示解析を画面の外で動かす補助処理は `DisplayAnalysisSupport.swift` に分けています。表示解析の計測と別タスク実行を、画面本体から分けて扱うためです。
- 解析表示の完了状態、実行中状態、失敗状態は `DisplayAnalysisStateStore.swift` に分けています。入力、補正後、最終版ごとの表示解析状態をまとめて扱うためです。
- 補正とマスタリングの進捗状態は `ProcessingProgressStateStore.swift` に分けています。実行中、完了、スキップ、失敗の状態更新を同じ仕組みで扱うためです。
- 補正設定、マスタリング設定、解析モード、処理時に固定した設定は `ProcessingSettingsState.swift` に分けています。設定変更と処理状態を分けて扱うためです。
- 新しい音源や新しい処理結果を扱う時は、古い表示解析タスクをキャンセルします。補正後のマスタリング解析とノイズ確認がそろうまでは、マスタリング開始ボタンを押せないようにしています。
- 補正中またはマスタリング中は、同じツールバーボタンとメニューバーの `処理` メニューからキャンセルできます。キャンセル要求は画面だけで終わらず、別スレッドの音声処理へ渡し、次の主要工程へ進む前に停止します。途中まで作られた出力ファイルと無効になった波形・解析プレビューは消去し、キャンセルを失敗として表示しません。
- 試聴状態は、入力、補正後、最終版ごとに再生位置を持ちます。A/Bを切り替えても同じ時間から聞けるようにします。波形のクリックは再生位置を変更し、拡大中のドラッグは表示範囲だけを動かします。ドラッグ中は自動追従を止め、離した後は再生位置への追従へ戻します。表示範囲の移動では音声再生を止めたり作り直したりしません。
- 補正ログとマスタリングログは1行ずつ配列で保持し、通常画面では最新80行だけを描画します。ログ全文は内部で残しつつ、長いログ全体を毎回描き直さないためです。
- 比較メトリクスでは、周波数帯の範囲を先に決めておき、解析中に余計な配列を作りすぎないようにしています。
- 再生中スペクトルでは、音声の読み込み時に `Accelerate` で0.1秒ごとの周波数の強さを計算して保存します。再生中は、共通再生位置に対応する入力、補正後、最終版のうち、選択中のA/B比較に含まれる2音源だけを同じ時刻で表示します。曲全体の平均値ではありません。
- 再生中スペクトルの画面表示は `Swift Charts` ではなく `Canvas` で描きます。再生中に値が更新されても重くなりにくくするためです。Y軸は固定したdBFS目盛りにし、測定点を滑らかな曲線でつないで周波数ごとの変化を見やすくします。
- ベクトルスコープとラウドネスメーターは、同じ `AVAudioEngine` の音声受け取り口を使います。Lissajous は横方向を `(右 - 左) / 2`、縦方向を `(左 + 右) / 2` として計算し、菱形の枠で表示します。Polar Sample は横方向を `(右 - 左) / 2`、縦方向を `abs((左 + 右) / 2)` として半円型に表示し、45度安全ラインで位相ずれを確認します。Polar Level は短い区間の中央成分と左右差成分の平均から線を作り、線の角度でステレオ位置、線の長さで振幅を確認します。別の音声受け取り口を増やさないため、再生処理へ余計な負担を重ねません。
- ベクトルスコープの計算は音声再生を止めない別の処理列で行い、画面の更新だけをメイン処理へ渡します。履歴は薄く残し、停止、自然終了、ファイル変更では古い軌跡を消します。一時停止では、停止した瞬間の軌跡を保持します。
- ラウドネスメーターも同じ `AVAudioEngine` の音声受け取り口を使います。別の受け取り口を追加せず、計算は別の処理列、画面更新はメイン処理で行います。
- 比較メトリクスでは、マスタリング解析を丸ごと再実行せず、必要な数値だけを直接計算します。
- 比較メトリクスでは、音量窓の計算に累積エネルギーを使い、同じ波形を何度もなめないようにしています。
- LUFS、True Peak、LRA、短い時間ごとの音量は `LoudnessMeasurementService` でまとめて測ります。表、グラフ、品質チェックで別々の基準にならないようにするためです。
- Integrated LUFSは、公式K-weighting係数、400ms窓、100ms間隔、絶対ゲート、相対ゲートを使って測ります。外部のEBU R128系メーターとの差を小さくするためです。
- Short-Term LUFSは、3秒前後の窓を公式K-weightingのエネルギーから計算します。単なるRMS dBではなく、LUFSとして読める値にそろえています。
- True Peakは、サンプルだけの最大値ではなく、4倍オーバーサンプリングのFIR補間でサンプル間ピークも確認します。画面では dBTP として表示します。
- 比較表のLRAは、48kHzのモノラルとステレオだけを対象に、3秒窓、0.1秒間隔、末尾1.5秒の無音、絶対ゲート、相対ゲートを使って測ります。正式な条件で測れない時は `--` と表示します。再生音声と保存WAVへ無音は追加しません。
- LUFSだけが必要な処理では、True Peak、LRA、短い時間ごとの音量を同時に計算せず、LUFSに必要な計算だけを実行します。
- FFT処理では、フレームごとの作業配列を使い回して、短い配列を何度も作らないようにしています。
- 逆FFT処理でも、フレームごとの作業配列を使い回して、波形へ戻す処理を軽くしています。
- foldover用の逆FFTでは、全体を毎回ゼロにせず、使った周波数ビンだけを片付けます。
- スペクトログラムは、フレームごとの小さな配列をたくさん持たず、1本の連続した配列で持ちます。
- 解析と表示では、スペクトログラム全体の大きな二重配列を作らず、必要なフレームやビンだけを読みます。
- 解析で使う小さい中央値フィルタは、結果を変えずに短い配列生成を減らしています。
- foldover系の高域補完では、全フレーム分の空スペクトログラムを作らず、1フレームずつ逆FFTへ流します。
- 比較メトリクスの非同期処理では、波形系、チャンネル系、FFT系を大きな単位で並列実行します。
- ノイズ除去では、ビンごとの固定係数を先に計算し、mask式の結果を変えずに重複計算を減らしています。
- ノイズ除去のmask適用では、フレーム配列を直接参照して、同じ式のまま添字アクセスを減らしています。
- ノイズ除去の高域保護量は、フレームとビンごとに先に計算し、mask適用中は同じ値を配列から読みます。高域保護の式と出力は変えていません。
- ノイズ除去の低域保護は、強い演奏中フレームと余韻フレームを分けます。余韻フレームでは60Hz〜150Hzと150Hz〜250Hzだけ最低mask量を上げ、20Hz〜60Hzや250Hz以上は大きく戻さないようにしています。
- ハム測定では、50/60Hzと倍音の周辺比の判定式を変えず、同じフレーム内の同じ周波数だけを1回測って使い回します。STFTのreal/imag全体は保持せず、1フレームずつ周辺比を集計します。
- ハム除去では、静かな区間は今まで通り削り、演奏中は削り量を弱めます。ただし、50/60Hz系の中心周波数が周辺より明確に大きい時は、演奏中でもハムとして削ります。
- 低域ゴロゴロ整理では、60Hz〜150Hzだけフレームの強さで削り量を変えます。静かな区間は通常どおり、余韻は中間、演奏中は控えめに削ります。20Hz〜60Hzと150Hz以上は従来どおり扱います。
- パーセンタイル計算では、返す値を変えず、全体を並べ替えずに必要な順位の値だけを選びます。
- ノイズ測定の帯域通過フィルタでは、highPassとlowPassの式、順番、4回反復を変えず、作業用配列と係数を使い回して同じ値を返します。
- ノイズ除去では、10kHz〜16kHzの前後フレームを比べ、急に出る高域の粒子感だけを少し安定させます。
- 診断用の出力先を渡した時は、ノイズ除去の8kHz〜12kHz、12kHz〜16kHz、16kHz〜20kHzについて、raw mask、granular mask、shimmer mask、最終maskが何dB下げたかをログと診断レポートに出します。
- 補正後高域保持は、全体音量との比率ではなく、8kHz〜12kHz と 12kHz〜16kHz の実レベル低下を見て、16kHz〜20kHz が戻りすぎない候補を選びます。
- 補正後高域保持では、まず補正候補そのものを安全確認します。問題がなければ、音を薄めた候補は作らず、同じ音の結果を保ったまま余計な計算を減らします。
- 補正後高域保持では、原音側の4つの高域測定を先にまとめて計算します。補正中に変わる音側は、その時点の音を今まで通り測ります。
- マスタリングのノイズ戻りガードは、ラウドネス補正後の高域ピークだけで判断せず、静かな区間のヒス床と短時間だけ不安定に出る高域を分けて判定します。
- 補正にも詳細設定があります。弱い、標準、強いの各プリセットが初期値を持ち、補正の強さ、原音保持、低域整理、中低域整理、高域補完、高域の自然さなどを調整できます。
- 補正ログには、ノイズ除去だけで10kHz〜16kHzのチラつき、12kHz以上、16kHz以上、18kHz以上が何dB変わったかを表示します。
- 画面の比較エリアには、ノイズチェックも表示します。ヒスは静かな区間の8kHz以上、サ行は5kHz〜9kHzの瞬間差、ハムは50/60Hzと倍音の周辺比のように、種類ごとに測り方を分けます。
- ノイズチェックの各行では、入力、補正後、最終版の実測値、補正での改善量、マスタリングでの戻り量、総合判定を同じ横棒グラフの中で確認できます。
- マスタリングでは、ハーシュネスが高く、かつ高域戻りも強い素材だけ、10kHz以上を弱く抑える高域戻りガードを入れています。
- マスタリング解析では、STFTの全フレーム結果を大きな配列として持たず、1フレームずつ読みながら帯域レベルと刺さり感を集計します。Metal対応Macでは、数百フレームごとにMetalへ渡して同じ結果を出します。使えない時だけCPUで同じ計算をします。
- マスタリング解析のラウドネス計算では、中間配列を減らして同じ結果のまま軽くしています。
- マスタリング解析のステレオ幅計算では、和差の配列を作らず、左右の二乗和と内積から同じ値を出しています。
- マスタリング解析では、STFTと帯域集計、ラウドネス、トゥルーピーク、ステレオ幅の工程別ベンチを取り、マスタリングログにも表示します。
- マスタリング解析では、実音源でも工程別ベンチを取り、どの工程が一番重いか確認できます。
- マスタリング解析の帯域集計では、Metalを本処理に使い、ログでは `STFT+帯域集計(Metal)` と `帯域集計(Metal)`、またはCPU版の名前で表示します。
- トゥルーピーク解析では、補間式を変えずに配列生成と内側ループの余分を減らしています。
- 補正処理では、左右チャンネルの重いSTFT処理を並列に進めて待ち時間を減らします。
- 補正処理では、読み込み、解析、低域ノイズ整理、ノイズ除去、サ行保護、再解析、高域修復、低中域残り、ピーク保護、書き出しの工程別ベンチを取れます。
- 入力ファイル選択時に作った補正用解析結果は、解析モードが同じなら補正開始時に使い回します。解析がまだ無い時や解析モードが変わった時は、従来通り補正内で解析します。
- 入力や補正後のノイズ測定値がすでにある時は、補正の低域整理・シマー制限、マスタリングのノイズ戻りガードで使い回します。
- 補正とマスタリングは、開始直後に素材を診断し、各工程を `実行 / 軽量 / スキップ` に分けます。
- スキップした工程は、ログと進行状況の両方に表示します。
- 診断用の出力先を渡した時だけ、補正とマスタリングの工程ごとの中間WAVを書き出せます。
- 中間WAVは、どの工程で高域、空気感、音量感が変わったかを、実音源で確認するために使います。
- 進行状況はログの文章ではなく、処理側から出す工程イベントで動きます。文章を少し変えても、進行表示が壊れにくい作りです。
- 補正ログとマスタリングログは、専用のログ状態ストアで全文と画面表示用の直近80行を管理します。
- 長い工程では、代表区間の確認回数などを進行状況にも出し、止まって見えないようにします。
- 補正完了とマスタリング完了では、設定の「完了通知」がONの時だけ、音付きローカル通知を1回だけ送ります。新規利用者の初期値はOFFで、利用者がONにした時だけmacOSへ通知許可を求めます。アプリ起動直後には許可を求めません。
- `ノイズ除去`、`高域修復`、`ピーク保護`、`ラウドネス` は安全上の重要工程なので常に実行します。
- 高域修復後のシマー保護は、修復でヒス、サ行、シマーが実際に増えた時だけ実行します。
- サ行/シマー保護は、5kHz〜9kHzのサ行、6kHz〜10kHzの刺さり、10kHz〜14kHzのチリつきを別々に測り、前後フレームより短時間だけ大きい部分を下げます。
- 8kHz〜12kHzの持続する煌びやかさと、12kHz〜16kHzの空気感は、サ行/シマー保護で常時削らない設計です。
- プレビュー用の一時ファイルは `VelouraLucentPreview` に作り、新しい入力選択時と画面終了時に掃除します。
- 画面表示用の解析は、既に入力・補正後・最終版の結果がそろっている時は再実行せず、キャッシュ済みの結果を使います。
- 補正の低域ノイズ整理とシマー制限は、測定回数をログに出します。シマー制限は、静かなヒス床が低くても、8kHz〜14kHzで短時間だけ飛び出るチラつきがあれば実行します。
- シマー制限は、長い音源で全体測定を何度も繰り返さないよう、代表区間で軽く測り、強い補正時だけ最後に全体確認を1回行います。16kHz以上の空気感は戻して守ります。
- マスタリングのノイズ戻りガードも、長い音源で全体測定を何度も繰り返さないよう、代表区間で軽く測り、必要な時だけ最後に全体確認を1回行います。
- マスタリングのノイズ戻り候補を確認する時は、変化しない基準音の8kHz〜20kHzを候補ごとに測り直さず、先に測った3帯域の値を使い回します。候補音側は、その時点の音を今まで通り測ります。
- マスタリングのノイズ戻り判定は、共通の軽量測定と高域保護を `MasteringNoiseReturnSupport.swift` に分けています。最後の緊急ノイズ上限確認は `MasteringFinalNoiseReturnCeiling.swift` に分けています。音の計算順、しきい値、ログ、診断WAV名は変えていません。
- マスタリングでは、ヒスを抑えすぎて5kHz以上の明るさが大きく落ちないよう、最後に高域の残り方も確認します。
- 補正とマスタリングの内部判定は `InternalAudioJudgementPolicy` に主要なしきい値をまとめ、意味が違う判定値は別の定義として扱います。
- ノイズ測定値が欠けた時は「問題なし」と見なさず、再測定できる工程では再測定し、ルート判定では処理をスキップしない方向で扱います。
- 補正処理のピーク保護では、音量を持ち上げず、クリップしそうな部分だけ安全側へ抑えます。
- 補正処理の高域補完では、12kHz〜16kHzの落ち込みだけでなく、16kHz以上がすでに残っているか、18kHz以上に荒れが多いかも見て、補完量を調整します。
- 帯域処理では、帯域エネルギーとgain計算の作業配列を使い回し、短い配列生成を減らしています。
- `STFT/ISTFT` の往復削減は音質リスクが高いため、先に `Docs/STFTISTFTRoundTripReductionDesign.md` で設計を固定しています。
- 解析のMetal/GPU化は、先に `Docs/AudioAnalysisMetalGPUDesign.md` で実験モード前提の設計を固定しています。
- `temporal median` のMetal化は、先に `Docs/TemporalMedianMetalDesign.md` で一致条件と採用条件を固定しています。
- 解析には `自動`、`安定CPU`、`実験Metal` の入口があり、UIから選べます。初期値は自動です。
- 補正用解析では、STFTのreal/imag全体を持たず、周波数ごとの大きさだけを保持して解析します。実験Metalでは `temporal median 17` をMetalで試作しています。マスタリング解析の帯域集計は、Metal対応Macでは本処理でMetalを使います。
- 用語は正式名称へ寄せて、`?` ボタンから読み方と意味を確認できます。
- ブラウザではなく、Mac の普通のアプリとして動きます。
- 画面は Apple の Liquid Glass UI 方針に合わせ、`macOS 26` 以上を前提にしています。通常ウィンドウでは、背後のデスクトップや別ウィンドウが透けて見える透明な土台として扱います。フルスクリーンまたはmacOSの「透明度を下げる」が有効な時は、ウィンドウを標準の `windowBackgroundColor` で不透明にします。右設定のアプリ設定では、通常ウィンドウの背景だけを `0%` の完全透明から濃くする「透明感」と、背景ぼかしの5段階調整を別々に保存できます。ぼかし調整がOFFなら従来と同じ`thin`を使い、ONなら5段階から選んだ素材を使います。OFFにしても選択した段階は保持します。フルスクリーン中は、これらの調整を無効にします。「透明度を下げる」の切り替えでは保存済みの値を変更せず、無効に戻すと元の透明感とぼかし設定へ戻します。
- 画面全体は、Rootが1個だけ所有する共通外枠の中で、左の入力と処理状態、中央の横長波形・再生中スペクトル・ベクトルスコープ・スペクトログラム、右の確認値と詳細設定、中央下の直近ログと全体工程に分かれます。通常／Stemを切り替えても外枠は作り直さず、各場所の中身だけを交換します。
- 上部ツールバーでは、音声選択、補正実行、マスタリング実行、書き出しを中央の処理操作としてまとめます。Stem Modeだけは、補正とマスタリングの間に再ミックス実行を表示します。通常／Stemの音声選択は同じMac標準ファイル選択処理を使います。再ミックスはスライダー、マスタリングはチェック付き波形のSF Symbolsで区別し、補正、再ミックス、マスタリングがキャンセル表示になった時は文字と×アイコンの両方を赤くします。通常／Stemとも、キャンセル受付後は「キャンセル中...」へ切り替えて処理が止まるまで同じボタンを再操作できません。Macのウィンドウ上部にはタイトル文字を表示しません。左サイドバーの開閉は、標準ツールバーボタンを使わず、赤、黄、緑のウィンドウボタンの右隣に背景なしのアイコンだけで表示します。右設定の表示切り替えは白い背景付きのツールバーボタンにせず、左サイドバー開閉アイコンと近い高さ・大きさのアイコンだけで右上に表示し、右側のスクロールバーに重ならない位置へ置きます。
- 詳細設定は右側の設定パネルにまとめています。右パネルの先頭は `詳細設定` の見出しを出し、その下に `補正 / マスタリング / アプリ` の切り替えを表示します。補正プリセットは同じ文言のラベルを重複表示せず、弱い、標準、強いの切り替えを表示します。補正の基本、掃除と修復、上級、マスタリングの基本、マスタリングの音色、マスタリングの上級はロータリーノブで表示し、それ以外の詳細設定は右側で読めるように1項目ずつ縦に並べています。
- 右設定パネルは必要な時だけ表示できます。閉じると中央作業画面が広がり、左側の入力と処理状態は表示されたまま残ります。
- 左サイドは通常モードで使っていたmacOS標準の分割表示を共通外枠に1個だけ置き、220〜300 ptの範囲（基準260 pt）で変更できます。右設定パネルは440 pt、中央作業画面は620 pt以上で残り幅を使います。左右の表示・非表示と共通の分割表示はRootが保持するため、通常／Stem切り替えで分割表示を作り直しません。
- 試聴比較は中央上部にまとめています。入力、処理後、最終版を聞き比べ、A/B比較、再生、一時停止、停止、音量、ラウドネス合わせを同じ場所で操作します。通常モードの処理後は「補正後」、Stem Modeは再ミックス前なら「補正済み純粋加算」、再ミックス後なら「Stem再ミックス」と、実際に選ばれる音源名を波形、比較対象、再生状態、再生中スペクトル、ベクトルスコープ、スペクトログラムへ共通表示します。比較対象の説明文は、画面幅で位置を変えず、`比較対象` ラベルの右側、比較対象ボタン列の上に固定して表示します。再生操作の中では、`A/B切替` の右隣に `現在 A` または `現在 B` を色付きガラスチップで表示します。波形の外枠、再生操作、再生・一時停止ボタン、A/Bラベルは、透明ガラスだと分かる見た目にしています。波形は16,384区間の正負ピークとRMSを保持し、各区間を縦方向の最小値〜最大値として描くため、拡大時にも最大値と最小値を結ぶ線が交差しません。各段の時間目盛り、カーソル位置、連続ズームの表示範囲を共有し、カーソル位置の時刻を波形上で確認できます。拡大の中心は現在の再生位置です。カーソル位置は時刻確認とクリック・ドラッグ操作にだけ使用します。最大倍率は波形データ数と表示幅から決まり、拡大中は波形ドラッグで共通表示範囲を移動できます。`全体表示`だけが全曲表示へ戻し、新しい入力音源を選んだ時も全曲表示へ戻します。
- 左側のファイル名と保存場所は、長くてもサイドバーを押し広げないよう途中を省略して表示します。マウスを重ねると完全な保存場所を確認できます。
- 中央上部には `Veloura Lucent` の大きい見出し、説明文、`基本表示 / 詳細解析` の切り替えを、上に空きすぎた余白を作らず固定表示します。選択中の画面名はこの切り替えで分かるため、その下へ同じ「基本表示」「詳細解析」の見出しや説明を重ねず、実際の波形または解析内容から表示を始めます。`基本表示` では、波形、再生中スペクトル、ベクトルスコープ、スペクトログラムを同じ縦の流れで表示します。
- `詳細解析` では、入力、補正後、最終版の主要数値、補正差分、マスタリング差分、ノイズ7種類、ステレオ相関、時間ごとのステレオ相関推移、短時間ラウドネス、ダイナミクス推移、平均スペクトル、周波数帯域詳細を確認できます。
- 通常モードとStem Modeの詳細解析は、同じ表示専用部品へ各モードの実測結果を渡します。通常モードは入力、補正後、最終版、Stem Modeは入力2mix、現在の処理後音源、Stem Mode最終版を同じ順序、単位、差分表示、ヘルプ、カーソル表示で確認します。Stem Modeの処理後音源は、再ミックス前は補正済み純粋加算、再ミックス後は実行済み再ミックスです。Stem Modeだけに必要なraw／補正後Stem、役割別解析、DSPごとの役割別guard、raw再ミックス／純粋加算／再ミックスの比較、再合成・残差・帯域・ノイズ測定は、その共通表示の下へ追加します。音声処理や品質合否を表示部品へ移さず、既存の解析・DSP・guardが作った結果だけを表示します。
- Stem Modeの`Stem固有解析`と`再ミックス固有解析`は、詳細解析内の他の開閉項目と同じ`analysisCard()`と`DisclosureToggleButton`を直接使い、専用の複製カード装飾を持ちません。同じ全幅ガラス背景、丸い開閉ボタン、`?`ヘルプで表示し、最初は閉じています。開いた本文は小さい固定文字ではなく、macOSの文字サイズへ追従する`callout`以上で表示します。
- `Stem固有解析`は詳細ログのコピーではありません。raw／補正後の測定値、役割別解析、各DSPの最終適用結果、役割別guard、rawへ戻した理由を結果表として表示します。routeを決めた詳しい理由、処理順、処理時間、保存・検証の経過は、時系列の詳細ログで確認します。
- ステレオ相関は、上段で曲全体の値を見ます。下段では、曲のどの時間で左右の音が逆向きに近づいたかを見ます。0より下の時間帯は、モノラル再生で音が痩せる可能性があります。無音区間は相関値として計算せず、推移線もそこで区切ります。横軸は音源全体の長さで表示するため、末尾の無音区間も線がない時間として残ります。モノラル音源では推移グラフではなく説明文を出します。
- 時間ごとのステレオ相関推移、短時間ラウドネス推移、ダイナミクス推移、平均スペクトル比較、スペクトログラムでは、グラフ内へカーソルを置くと縦線を表示し、グラフの上に確保した専用行へ、その位置の時間または周波数と解析値を表示します。数値はグラフの線や色へ重ねません。再生位置の変更や再解析は行いません。
- `仕上がりの方向` は独立カードに戻さず、ラウドネス、True Peak、ダイナミクスは主要数値比較へ、煌びやかさ、空気感、超高域、こもりは周波数帯域詳細へ統合します。良否や注意は右側の品質警告で確認します。
- 詳細解析の表は、右設定パネルや下部ログへ重複して出しません。右側の「解析結果と品質確認」は通常／Stemで同じ表示部品を使い、音源選択、主要4値、ヘルプ、品質警告、完了レポートの並びと操作を共通にします。Stem固有の重要警告だけを共通表示の追加欄へ渡します。下部は直近ログと全体工程を担当します。
- 左側は狭いサイドバーなので、各音源のファイル情報を音源行の中に入れます。Stem Modeでは入力、純粋加算、再ミックス、最終版を別々に表示します。補正、再ミックス、マスタリングの工程一覧、実行時間、進捗率は `工程` にまとめ、通常／Stemとも同じ`SidebarProcessingStatusListView`で表示します。Stem補正ではドラムのraw基準アタック保護を独立工程として表示し、再ミックスではgain、条件付き帯域制御、pan、send、共通reverb、dry／return加算、保存、検証を実処理順に表示します。
- 試聴の音量、ラウドネス合わせ、現在の比較対象は、横幅が足りない時に上下へ並びます。通常の3音源比較、Stemのraw／補正後比較、純粋加算／再ミックス比較は、同じ`AudioWaveformWorkspaceView`へ表示する波形名と比較名だけを渡します。3つとも波形、再生・一時停止・停止、A/B切替、音量、ラウドネス合わせ、シーク、パン、ズーム、高さ変更を同じ操作で使います。
- 補正中やマスタリング中に触ると危ない操作は、ツールバーや設定画面でも押せないようにしています。通常モードでは、ツールバーの実行可否と詳細設定の編集可否を別々に判定し、補正またはマスタリングの実行中だけ設定を無効にします。Stem Modeも同じ規則を使い、補正、再ミックス、マスタリングの実行中だけ3つの設定を無効にします。処理が止まれば設定を編集でき、必要な入力・成果物が残っていれば各工程を再実行できます。
- メニューバーから、現在の通常／Stemに応じた音声ファイル選択、補正、マスタリング、書き出し、2mixの再生・一時停止、停止、A/B切替、右設定パネルの表示切り替えを実行できます。Stem Modeの時だけ、再ミックスも実行できます。表示メニューの `モード` から `通常補正` または `Stem Mode` を選ぶと、上部のモード切り替えと同じ処理でアプリ本体の左・中央・右・ツールバーを切り替えます。Stem個別raw／補正後試聴は専用操作のままです。音声ファイルを開く操作は `Command-O`、補正は `Command-R`、Stem再ミックスは `Option-Command-R`、マスタリングは `Shift-Command-R`、再生・一時停止は `Space`、停止は `Escape`、A/B切替は `Command-B`、右設定パネルは `Option-Command-I` に対応します。左サイドバーはmacOS標準のサイドバーコマンドから切り替えられます。ツールバーボタンは残し、マウスとキーボードの両方からRootが所有する同じ操作を呼びます。
- アプリの既定言語は日本語です。独自の `処理`、`再生`、`書き出し` だけでなく、macOSが用意するファイル、編集、表示、ウインドウ、ヘルプの標準メニューも日本語で表示します。
- ロータリーノブは、実際のノブ外周に合う円形領域をドラッグして調整します。プラス・マイナスボタンとフォーカス中の上下左右キーは、押し続けて連続変更できます。ドラッグ、ボタン、キーで操作している間は、タイトル左の丸を緑で表示します。VoiceOverの調整操作にも対応します。
- 補正、マスタリング、書き出し、入力ファイル解析に失敗した時は、利用者向けの原因説明と確認方法を標準アラートへ表示します。処理側から返った詳しいエラー内容はログへ残します。補正とマスタリングの完了、失敗、キャンセルはVoiceOverにも読み上げます。
- Liquid Glass の見た目は、表示状態に合わせて種類を切り替えます。通常ウィンドウでは従来の透明な見た目を維持するため `clear` を使います。フルスクリーンでは、単色背景の上でも境界、光、影が見える `regular` へ切り替えます。グラフ上の案内表示など、内容へ直接重なる小さな表示は、どちらの表示状態でも `clear` を使います。左サイドバーは全面ガラス板にもセクションごとのカードにもせず、見出し、余白、区切り線で情報を並べます。波形本体やスペクトログラム本体には強い透明効果を重ねず、読みやすさを優先します。
- 透明ガラスUIでは細い小文字が読みにくくなるため、通常の説明文、ログ、工程名、補助ラベルは `caption` 相当の小さい文字を避け、原則として `callout` 相当の大きさで表示します。目盛りのように面積が限られる文字だけ、最低でも `caption` 相当にします。
- `基本表示 / 詳細解析`、`入力 vs 補正後 / 入力 vs 最終版 / 補正後 vs 最終版`、`Polar Sample / Polar Level / Lissajous`、`補正 / マスタリング / アプリ`、`弱い / 標準 / 強い`、`解析モード`、`入力 / 補正後 / 最終版` の切り替えは、同じ幅基準のLiquid Glassボタンで表示します。選択中は目立つガラス、未選択は通常のガラスで表示します。

## 技術アーキテクチャの概要
- Stem Modeの現在の処理境界は、`補正 → 任意の再ミックス → マスタリング`の3段階です。補正段は補正済み4Stemと、gain・pan・reverb・normalization・dynamics・limitingを行わない`corrected-pure-sum-48000.wav`を保存して`readyForRemix`で止まります。再ミックス段は今回のraw／補正済みStemから求めた自動値と、利用者が変更した項目だけの手動上書きを使い、`stem-remix-48000.wav`を保存して`readyForMastering`で止まります。同じセッションで自動値が更新されても手動項目は残り、新しい補正セッションの開始時だけ初期化します。マスタリング段だけが既存saturation、loudness、True Peak保護を適用します。通常モードの処理経路にはこの3段階を追加しません。
- 画面: `SwiftUI`、Rootが所有する共通`NavigationSplitView`と`WorkspaceShellView`、中央作業画面、開閉式の左右パネル、`toolbar`
- Stem Modeの分離基盤: アプリはreview済みの`demucs-mlx-swift`へ直接つなぎ、そこから必要なMLX関連部品を使います。`MLX Swift 0.30.6`、`swift-transformers 1.1.6`、`swift-jinja 2.3.2`などの正確な組合せは`Package.resolved`で固定します。ルートの`Package.swift`へ同じ部品を重ねて宣言すると未使用警告になるため、重複宣言は置きません。AIモデル2資産は確認後取得、`mlx.metallib`は署名アプリ同梱へ分離しています。通常／Stemの切替、モデル取得・復旧、分離、Stem別補正、内部診断用raw再ミックス、補正済み純粋加算、任意再ミックス、既存Mastering、Stem単位の原子的確定をStem専用作業画面へ接続しています。通常モードの公開処理は変更していません。
- Stem Modeのモデル取得: `htdemucs.safetensors`と`htdemucs_config.json`は、40桁revisionを含む固定URLから取得します。初回、修復、完全再取得の前に、取得元、2ファイル、合計168,007,757 bytes、revision、MIT License、通信開始を利用者へ表示します。承認前には通信しません。
- Stem Modeのモデル検証: 取得した2資産はApplication Supportのstagingへ置き、`X-Repo-Commit`、完成容量、SHA-256、config契約、receiptを全件確認します。その後、同じvolume内で2資産一式を検証済み世代として原子的に有効化します。時限付き転送先URLは保存しません。失敗や中断では以前の検証済み世代を残します。
- Stem Modeの実行資産: `mlx.metallib`はモデル取得と切り離し、`mlx-swift_Cmlx.bundle/Contents/Resources/default.metallib`へ1個だけ置いて署名アプリへ同梱します。AIモデル2資産はアプリへ同梱せず、`mlx.metallib`用の外部配布先も作りません。
- アプリ資産の場所: SwiftPMで直接実行する時と署名済み`.app`ではresource bundleの場所が異なります。画像、manifest、同梱MLX資産は`AppResourceBundle`から取得し、本番アプリで開発用`Bundle.module`の探索へ戻らないようにします。
- Stem Modeのモデル読み込み: 右サイド「アプリ」タブの「Stem分離」がApplication Supportのactive世代と同梱MLX資産を検証し、音楽処理へ確認済みinstallationを渡します。モデルが未取得や破損でもStem Mode画面は表示し、入力選択と入力表示解析は利用できます。補正だけをモデル利用可能まで無効にします。音楽処理はactive世代の再読込、容量・SHA-256、MLX runtimeの再検証を行いません。確認済みモデルを実際に開けない場合は、選択入力と入力表示解析を維持して補正処理を失敗とし、右サイドのモデル状態へ案内します。Hubや別revisionへ黙って取りに行きません。
- Stem Modeの入力: 既存の48kHz化を前提とする`AudioFileService.loadAudio(from:)`を分離入力へ流用しません。channel layoutを保持する専用変換で、mono、stereo、役割が明確な標準multichannelを44.1kHz / stereo / Float32へ変換します。明示的なunknown／discreteは1ch・2chを含め自動mono／stereo扱いせず、現在のlayout identityと一致する「入力channel行×stereo出力列」の正確な係数matrixを利用者が確認するまで開始しません。
- Stem Modeの補正: 通常モードのファイル入出力と`AudioSignal`補正コアを分け、解析、ノイズ測定、下位DSP、DSP内部guardを共用します。vocals / drums / bass / otherは独立した補正プリセットと12設定値を持ち、各roleの設定だけを該当correctorへ渡します。raw Stemは44.1kHzの正本として保持し、連続補正段では各Stemを補正境界で1回だけ48kHzへ変換します。同じ48kHz処理信号を通常モード共通解析、時間整列した役割別解析、既存route、下位DSPへ渡します。routeの`run` / `light` / `skip`を先に確定し、各DSPと内部guardが返した1本へStem役割別guardを適用します。問題区間のDSP差分だけを、今回のraw StemとDSP前後の同一時間位置における相対量で弱めます。`preserveMinimum` / `preserveStability`やIQRは本番guardへ使いません。解析不能またはDSP失敗時はそのDSP直前Stemを維持して次工程へ進みます。bassの`HumRemover`と`RumbleReducer`はStem側から個別にguardし、片方の問題で両方を取り消しません。1roleのcorrector全体を完了できない時だけ、そのroleを48kHz rawとして保持します。Stem専用の完成候補、候補比較、AI品質選択、16通りの組合せ、Stem個別limiterは作りません。
- Stem Modeの役割別実測: 所有AI生成音源は、解析値取得、DSP前後の接続、NaN／Infinity、再現性、局所fallback、再ミックス成立の開発検証にだけ使います。サンプル値から本番閾値、固定mapping、DSP適用条件、品質合否を作りません。
- Stem Modeの再ミックスとキャンセル: 「補正を実行」は一本道のStem補正後、補正済み4Stemと純粋加算を検証・保存・表示して止まります。「再ミックスを実行」は、Stem別gain、pan前の条件付き帯域ducking、Stem別pan、pan後のStem別send、1基の共有reverb、dry／return加算の順で処理します。帯域duckingは有効・無効と処理量を別々に自動・手動設定できます。固定-3 dB、normalization、saturation、limitingは行いません。「マスタリングを実行」は検証済み再ミックスだけを既存Masteringへ渡します。補正キャンセルは今回のStem一時WAVを削除します。再ミックスの失敗・キャンセル・設定変更は補正済み4Stemと純粋加算を保持して再実行待ちへ戻し、マスタリングの失敗・キャンセルは再ミックスまでを保持して最終版だけを削除します。
- Stem Modeの状態管理: 処理状態、設定、解析結果は現在起動中のメモリだけに保持します。永続run、checkpoint、workflow schema、設定／解析JSON、保存履歴、起動後再開、「保存して中断」はありません。補正・マスタリングの完了通知は完了時に直接送り、未処理イベントを別状態として保持しません。
- 配布用リソース: 配布アプリは `Contents/Resources` の正式な場所だけを使い、AIモデル2資産を含めません。`default.metallib`、モデル契約manifest、MIT License表記、第三者noticeを置いてからアプリ全体を署名します。アプリ直下には `Contents` 以外を置きません。現在の実測はローカル検証用のad-hoc署名で、Developer ID署名、notarization、Gatekeeper受入れは後続工程です。
- Stem Modeの2026-07-14実測: 現行2資産取得配置で、Release build、配布resource、AIモデル2資産の非同梱、`default.metallib` 1個の容量・SHA-256、第三者notice、ad-hoc署名、起動とプロセス継続まで確認済みです。当時の旧UIでは、モデル欠落時の復旧画面、取得前確認、取得進捗、固定revision取得、2資産の容量・SHA-256、schema v2 receipt、active化まで確認しました。この旧復旧画面は現在廃止され、同じ管理機能を右サイド「Stem分離」へ統合しています。現在UIの実画面確認済み根拠には使用しません。
- Stem Modeの旧取得画面警告: 原因は、ダウンロードのbyte進捗まで含む`operationState`全体をSwiftUI画面が高頻度で監視していたことでした。当時は、アプリ全体のRootが「取得中か」だけ、旧復旧画面が「取得確認待ちか」だけを見るように修正しました。現在は専用復旧画面を廃止し、同じ取得状態を右サイド「Stem分離」へ表示します。実モデルの再ダウンロードによる現在UIの実画面確認は、取得前の利用者確認を必須とする契約を守るため未実施です。
- Stem Mode工程5・6の基盤整理: 通常モードの保存型や通知enumへStem用caseを追加せず、Stem専用の保存DTO、一本道補正、構造検証、最終レポートadapter、完了通知serviceへ分離しました。工程5ではStem専用候補生成・比較・採用、再ミックス回復候補、No Vocals、自動gain previewをproduction経路から除去しました。位相、相関、残差、帯域、noise、peakは解析・表示・reportへ残しますが、自動棄却や選択には使いません。工程6では、音楽処理開始時のモデル再inspectionと、分離直前のactive世代・MLX runtime・容量・SHA-256再検証を除去し、確認済みinstallationを明示入力にしました。通常モードの補正・マスタリング・品質レポート・通知のproduction実装は変更していません。`ContentView.swift`は承認済みの画面所有境界だけを追加し、従来の`ContentView()`の動作と宣言を維持しています。工程5の対象109 tests、工程6統合対象80 tests、モデル管理対象88 testsと`swift build`は成功しています。終了時signal 11はテスト用`NSWindow`の自動解放とSwift所有権解放の重複が原因であり、製品側スクロール実装を変更せずテストwindowを`isReleasedWhenClosed = false`へ修正しました。検証追加後の通常条件でも全747 tests / 79 suitesが1854.229秒で失敗0・signal 11なしで完走しています。
- Stem Modeの三段階操作: 「補正を実行」は純粋加算の保存後に`readyForRemix`へ移ります。自動再ミックス値はraw／補正済みStemの実測から作り、利用者は再ミックス全体を自動または手動へ切り替えます。手動では変更した項目だけを自動値へ上書きし、自動へ戻しても手動値を保持するため、再び手動へ切り替えると前の調整へ戻れます。「再ミックスを実行」は再ミックス保存・検証後に`readyForMastering`へ移ります。「マスタリングを実行」はその再ミックスと実行時点のマスタリング設定を既存Masteringへ渡します。3工程とも処理完了後に再実行でき、上流工程を再実行した時点で後段成果物だけを更新待ちへ戻します。画面は純粋加算Aと再ミックスBを同じ時間位置で試聴でき、任意の再生時ラウドネス合わせは保存音声を変更しません。
- Stem Modeの工程表示とログ: 左サイドは、分離、各Stemの解析、既存DSP、ドラムのraw基準アタック保護、Stem保存、再ミックス、既存マスタリングの実イベントを順番に表示します。各工程行は1行の固定表示とし、行の中へ実行中ラベルや詳細文を追加して高さを変えません。現在の工程名、経過時間、進捗率は工程一覧の上部へ表示し、詳しい処理内容は下部ログへ表示します。工程の実行中、完了、失敗、待機の色と、左右パネルのタイトルバー開閉ボタンは通常モードの共通部品を使います。補正とマスタリングの「処理中」は通常モードと同じく、各段階の開始から完了・失敗・キャンセルまで維持します。個別工程の状態は現在の工程名と進捗の表示に使い、工程が切り替わる短い間に「待機」へ戻しません。今回のStem解析から決めたrouteが不要と判断したDSPだけを「省略」と表示します。DSP失敗、出力構造不正、Stem役割別guard不確実から処理直前Stemを安全に維持できた時は「完了」とし、理由を詳細ログとguard記録へ残します。表示用の進捗だけで音声処理を停止しません。下部の直近ログは内部ログ末尾ではなく、入力、補正、再ミックス、マスタリング、利用者の書き出しという操作単位で最大4件を表示します。詳細ログは通常／Stemで同じ外枠を使い、通常は補正・マスタリング、Stemは補正・再ミックス・マスタリングを分けます。状態更新用イベントは本文へ混ぜず、実行した主要工程の短い人向けログを使います。最終版の一時WAV生成は「書き出し」ではありません。外部保存できる成果物があれば「保存可能」、利用者が選んだ保存先へ書込みが成功した後だけ「保存済み」と表示します。
- 通常／Stemの画面切り替え、上部の入力・補正・マスタリング・書き出し、中央の基本表示・詳細解析は、同じLiquid Glass部品、hover表示、ヘッダー位置を使います。Rootはtoolbar、共通`NavigationSplitView`、`WorkspaceShellView`を各1個だけ所有し、通常／Stem切り替えでは処理先と左・中央・右の中身だけを交換します。外枠、左右開閉状態、window設定は交換しません。モード別の`NavigationSplitView`、複数の`.principal`項目、`ToolbarSpacer`、座標補正、幅履歴は使いません。Stem固有のモデル管理は上部toolbarや専用画面へ追加せず、右のアプリタブにある「Stem分離」で状態、検証、取得、修復、進捗を扱います。左右の開閉ボタンもRootが1つだけ所有し、通常／Stem画面の交換でtitlebar accessoryを削除・再追加しません。左右のボタンは同じ`LiquidGlassMotion.panel`とモーション低減設定を使い、0.24秒の`easeInOut`で開閉します。書き出しボタン全体は無効化せず、未生成の成果物だけをメニュー内で無効表示します。
- Stem Modeの詳細ログ: 入力音源を選んだ時点で、通常モードと同じ表示解析ログを補正列へ表示します。補正列は入力準備、分離、各Stem解析・DSP・guard、ドラムのraw基準アタック保護、補正済み4Stem保存、raw内部診断、純粋加算保存までを表示します。再ミックス列は自動値確定、gain、条件付き帯域制御、pan、pan後send、共有reverb、dry／return加算、保存、検証を表示します。マスタリング列は既存マスタリングログと最終版保存・検証・解析を表示します。3列とも制御イベントを本文へ混ぜず、長い内部理由は評価記録へ保持します。
- Stem Modeの右詳細設定: 通常／Stemの見出しとタブは同じ`InspectorSettingsSectionLayout`を使います。Stem補正は通常設定と同じカード・ノブ配置を直接使い、独自の外側`GroupBox`を重ねません。再ミックス設定も自動値の算出前から全体を表示し、未算出時は中立値であることを1か所だけ説明します。見出し、説明、カード、ヘルプは処理の実行可否に関係なく確認でき、操作不能条件は切り替え、つまみ、ボタンだけへ適用します。再ミックスは全体で1個の自動／手動切り替えを使い、その直下へ自動値へ戻す操作を表示します。「再ミックス調整」のヘルプでは、全Stemの音量が0 dB、パンが0、衝突回避が無効または0、リバーブ送信量と戻り量が0なら純粋加算とほぼ同じ音になることと、中立値以外では何が変わるかを説明します。Stemごとの音量、パン、共通リバーブ送信は通常設定と同じ`DAWKnobControl`で横に3個並べ、数値と単位を分け、つまみの全範囲を同じ距離で動かせる感度にします。各ノブは個別ヘルプを持ち、カード内では現在値を重ねて表示せず、自動判断の根拠を背景なしの補足文字で1回だけ表示します。Stem間の衝突回避は通常のスイッチ行とノブを使い、再ミックス実行は詳細設定へ重複させず上部ツールバーから行います。
- Stem Modeの処理理由表記: Stem Mode内で共通の補正判定、既存DSP、既存guardを使っていても、結果画面や処理ログへ「通常モードが処理した」と見える表現は出しません。「共通の補正判定」「既存DSP」「既存の補正後高域保持」のように、Stem Mode内での役割を表示します。通常モードで実績のある資産を再利用している設計上の事実は、画面の実行状態とは分けて資料へ記録します。
- Stem Modeの完了通知: 通常モードの通知型は変更せず、Stem専用通知serviceが補正完了とマスタリング完了を区別します。既存の「完了通知」設定がONの時だけ、各段階を同じrunで1回通知します。
- vendored source確認: `prepare_stem_models.sh`と`StemModelContractTests`はreviewed Demucs inventoryと実ファイルを同じ条件で比較します。macOSが自動生成する`.DS_Store`はソースではないため比較対象から除外しますが、他の未審査ファイルは従来どおりbuild・contract testを停止させます。
- Stem分離の再現性: 保存した分離seedはshift位置だけでなく、HTDemucsモデル生成時のMLX乱数にも使います。共有の乱数状態は変更せず、そのモデル生成中だけ有効なタスクローカル状態を使います。`fusedGroupNormGELU`は実モデルの大きいbatchでGPU実行値が壊れないようthreadgroup上限を256にします。2026-07-18の実モデル統合テストでは、同じ入力・固定Revision・seed・設定で2回生成した4Stemがsample単位とWAV SHA-256で完全一致し、GELU融合演算も実モデル形状4種で反復差0、標準MLXとの差は最大`9.536743e-07`でした。vendored sourceを変えた時は各fileのSHA-256、inventory自身のSHA-256、`prepare_stem_models.sh`と`StemModelContractTests`の固定値を一組で更新します。
- Stemマスタリングの一時WAV: 既存`MasteringService`はmacOS標準の`/var/folders/.../VelouraLucentPreview`へ一時WAVを保存します。`/var`はsystem symlinkなので、Stem一時WAVと同じ「親にsymlinkを一切許さない」検証をそのまま使うと正規の一時WAVまで拒否します。Stem側だけで、既存preview directory直下であること、実体がsystem temporary directory配下であること、leafがregular fileであることを字面と`realpath`の両方で確認してから読みます。通常モードの一時保存先は変更しません。
- ウィンドウ: 通常時は `NSWindow` を透明背景にし、SwiftUI 側のウィンドウ背景だけを表示します。「透明感」は背景層の濃さを変え、`0%` では完全透明にします。ぼかし調整がONなら、`ultraThin`、`thin`、`regular`、`thick`、`ultraThick`の5段階から選んだmacOS標準素材を使います。OFFなら、ぼかし追加前と同じ`thin`へ戻し、透明感の値だけをそのまま適用します。ぼかし素材には `materialActiveAppearance(.active)` を使い、アプリからフォーカスが外れても背景素材が非アクティブ表示へ切り替わらないようにします。フルスクリーンまたは「透明度を下げる」が有効な時は、AppKit側を不透明な `windowBackgroundColor` へ切り替えます。
- 左サイドバー開閉: Rootが共通`NavigationSplitView`の表示状態を持ち、`NSTitlebarAccessoryViewController`で赤、黄、緑のウィンドウボタンの右隣に背景なしの独自開閉ボタンを置きます。SwiftUI標準sidebarボタンは`.toolbar(removing: .sidebarToggle)`で外し、SwiftUIが後から同じ標準項目を追加した場合も`NSToolbar.willAddItemNotification`で検知して標準項目だけを除去します。
- スクロールバー: ウインドウ全体を探索せず、各 `ScrollView` のスクロールバーだけを細いOverlayにし、スクロール中だけ濃さを抑えて表示します。色は固定せず、macOSの外観へ適応します。「コントラストを上げる」が有効な時は、標準の濃さへ戻します。
- 音声ファイルの読み書き: `AVFoundation`
- 周波数ごとの計算: `Accelerate`
- 実行入口: `script/build_and_run.sh`

図にすると、こうです。

```text
Macアプリ(SwiftUI)
  -> VelouraCommands
    -> メニューバーから現在のウィンドウの操作を実行
    -> 表示メニューから通常／Stemのアプリ本体を切り替え
    -> 現在モードの音声選択、補正、マスタリング、2mix再生、書き出し、左右表示切り替え
  -> VelouraRootView
    -> 通常／Stem共通のメニュー操作とtoolbar操作を一度だけ組み立てる
  -> ProcessingActions
    -> 音声選択、補正、マスタリング、書き出し、キャンセルを実行
    -> 補正TaskとマスタリングTaskを保持し、画面終了時に停止
  -> DisplayAnalysisCoordinator
    -> 入力、補正後、最終版の表示解析Taskを管理
    -> 音源選択IDを確認し、古い解析結果を新しい音源へ反映しない
  -> UserFacingErrorPresentation
    -> 技術的なエラーを利用者向けの説明と確認方法へ変換
  -> AccessibilityAnnouncementService
    -> 完了、失敗、キャンセルをVoiceOverへ通知
  -> AudioFileInfo
    -> 入力、補正後、最終版の形式、サンプルレート、チャンネル、長さ
    -> 取得できる時だけWAVのビット深度
  -> RecentActivityEvent
    -> 選択、解析、補正、マスタリング、書き出しの直近処理
    -> 時刻、対象ファイル、解析値、進捗率
  -> NotificationService
    -> 完了時のローカル通知
  -> AudioProcessingService
    -> NativeAudioProcessor
      -> AudioFileService
      -> SpectralDSP
      -> 倍音/シマー解析
      -> foldover補完量推定
      -> ハム/低域ゴロゴロ整理
      -> ProcessingRoutePlanで補正ルートを決定
      -> ノイズ除去
      -> SpectralGateDenoiserでノイズ除去本体を実行
      -> サ行/シマー保護
      -> denoise後の再解析
      -> 最低限の高域修復
      -> 低中域の残りを軽く抑制
      -> シマー制限
      -> ピーク保護
    -> MasteringService
      -> LoudnessMeasurementService
        -> 公式K-weighting係数のLUFS / True Peak / 正式条件のLRA / 表示用の短期ラウドネスを測定
      -> MasteringAnalysisService
        -> 1回のFFT結果を帯域解析と刺さり感解析で共有
      -> ProcessingRoutePlanでマスタリングルートを決定
      -> MasteringProcessor
      -> MasteringSignalMathでマスタリング内の共通計算を担当
      -> MasteringHighFloorPreserverでマスタリング内の高域保持を担当
      -> de-esser
      -> 4バンドEQ
      -> 空気感
      -> 仕上げ
      -> 高域保持
      -> プリセット方針内の最終音量復帰
      -> 最終ノイズ確認
      -> 演奏中の150Hz〜500Hzを最終保護
      -> 最終音量上限確認
      -> 必要な時だけ短時間サ行ピーク制限
```

## コードベースの構造
- `Sources/VelouraLucent/App/`
  - アプリの起動です。Dock アイコンの上書きもここで行います。
  - `VelouraAppRuntime.swift` は、通常モードの処理、Stemモデル管理、Stem専用sessionを別々に所有します。通常補正・マスタリング中だけmode切替を止め、承認済みモデル取得中は利用者が通常モードへ戻っても取得を継続します。モードを切り替える時は、非表示になる側の試聴再生を停止し、切り替え先の再生とは重ねません。共有runtimeは画面やウィンドウが消えた時には停止せず、アプリ自体の終了時だけ停止します。
- `Sources/VelouraLucent/Resources/`
  - アプリ内で使う画像、Stem Modeのモデル契約、第三者ライセンスです。
  - `StemModels/stem-model-manifest.json` は、採用モデル、40桁revision、2つのstable download URL、AIモデル2資産の容量・SHA-256・Application Support保存契約、`mlx.metallib`の同梱契約、依存関係、音声入出力、Metal生成元を記録します。
  - `htdemucs.safetensors`と`htdemucs_config.json`はSwift Package resourceへ入れません。`mlx.metallib`だけを固定したMLXソースから準備し、配布アプリへ同梱します。大きな3資産はいずれもGitへ入れません。
  - `ThirdPartyNotices/` は、モデルと実行時依存が使うLICENSE、ACKNOWLEDGMENTS、model cardを同梱する場所です。`third-party-notices-manifest.json` が18ファイルの場所、サイズ、SHA-256を固定し、準備スクリプトと配布スクリプトが欠落や差し替わりを止めます。
  - `Rotary_Knob/2.png` は、右設定パネルのロータリーノブで回転するノブ本体です。金属の筋、光沢、青い点を含みます。
  - `Rotary_Knob/3.png` は、右設定パネルのロータリーノブで固定表示する土台です。数値枠、外周円弧、3点目盛り、`− / ＋` ボタン外観を含みます。
- `Sources/VelouraLucent/Views/`
  - 画面です。差分サマリー、スペクトログラム、比較表示もここです。
  - `VelouraRootView.swift` は通常モードとStem Modeの共通入口です。通常／fallbackの両方から同じRootを使い、mode切替だけで処理所有者を破棄しません。
  - `WorkspaceShellView.swift` は、左220〜300 pt（基準260 pt）の共通`NavigationSplitView`、中央620 pt以上、右440 ptの共通外枠と、中央の固定ヘッダー・スクロール本文・固定下部・詳細ログ切り替え、通常／Stem共通の下部寸法を担当します。
  - `StemModelManagementSection.swift` は、右サイド「アプリ」タブの「Stem分離」として、モデル状態、初回取得、修復、「モデル検証」（資産の再検証）、「モデル再取得」（AIモデル2資産の完全再取得）、取得前確認、進捗、中断、同梱runtime破損時の再インストール案内、`!`アイコンの分離情報を担当します。モデル状態は正常時も状態名・説明を省略せず、操作ボタンは右サイドの操作欄内で横並びにします。各ボタンは同じ横幅、高さ32 pt、中央寄せのアイコンと文字、上部ツールバーと同じLiquid Glassのホバー反応を持ちます。通常モードなどで使う小型ボタンは従来の大きさを維持します。専用モデル管理画面への切り替えは行いません。
  - `WorkspaceToolbarView.swift` は、Rootが常時所有する通常／Stem共通toolbarの表示を担当します。同じ位置のまま、現在のモードに応じて既存の入力、補正、マスタリング、書き出し処理へ接続します。
  - `StemModeWorkspaceView.swift` は、Stem専用の中央表示、入力drop、警告、画面の生存期間を担当します。sidebar、inspector、toolbar、window設定は所有せず、Rootの共通外枠から接続されます。
  - `StemModeCorrectionSettingsView.swift` は、通常モードと同じ12項目の表示定義、補正プリセット見出し、ヘルプ、戻す操作を使いながら、Stemごとの補正設定を変更します。中央のStem波形選択とは連動せず、補正処理の実行中だけ変更できません。
  - `StemModePreviewView.swift` は、入力2mix、補正済み純粋加算、再ミックス、最終版を扱う既存2mix試聴、純粋加算A／再ミックスBの専用A/B、選択中役割のraw／補正後Stem専用試聴を分けて接続します。A/B操作はDSP採否やマスタリング入力を変更しません。純粋加算、再ミックス、4つの`correctedStem`、最終2mixを書き出せます。
- `StemWaveformComparisonView.swift` は、右設定欄と同じ4つのStemを同じ順番と表示形式で示しますが、選択状態は共有しません。中央で選んだStemの分離直後rawと補正後Stemの波形、共通時間目盛りとカーソル位置、現在の再生位置を中心にした連続ズーム、再生、一時停止、停止、raw／補正後切替、同じ時間位置へのシーク、専用音量、再生時だけのラウドネス合わせを担当します。中央のStemを切り替えても右の補正設定対象は変わらず、右の設定対象を切り替えても中央の波形と試聴状態は変わりません。2mix試聴とは再生状態と拡大状態を共有せず、一方の再生開始時は他方だけを停止します。中央のStemを切り替えると波形だけ全体表示へ戻します。
  - `StemModeDetailedAnalysisWorkspaceView.swift` は、入力2mix、現在の処理後音源、最終版の共通比較に加え、raw／補正済みStem、既存routeの実行計画、9工程のDSP内部guard結果、Stem単位raw fallback理由、raw再ミックス／補正済み純粋加算／実行済み再ミックスの比較を表示します。候補比較や最終採用判断は表示しません。
  - `StemModeSidebarView.swift` は、入力、純粋加算、再ミックス、最終版と、補正／再ミックス／マスタリングの3工程ブロックを表示します。書き出しを処理ブロックにはしません。
  - `StemModeInspectorView.swift` は、右上のStem別補正、再ミックス、既存マスタリング、アプリ設定と、右下の「解析結果と品質確認」を担当します。解析結果欄は通常モードと同じ`InspectorAnalysisPanelContent`へStemの実測値と重要警告だけを渡します。未解析時は設定欄を圧迫しない小型状態表示を使います。解析モードは補正中だけ編集を止め、補正開始時に選択値を現在セッションへ渡します。
  - `StemModeFooterView.swift` は、直近4件、入力解析・補正・再ミックス・マスタリング・書き出しの進捗、補正／再ミックス／マスタリング別の詳細ログを担当します。
  - `ContentView.swift` は、通常モードの中央表示、入力drop、警告、画面の生存期間を担当します。sidebar、inspector、toolbar、window設定は所有せず、Rootの共通外枠から接続されます。
  - `InputAudioDropReceiver.swift` は、中央の作業画面へドラッグ中のファイルを確認します。音声ファイルなら通常の案内、音声以外なら警告案内を出します。補正中とマスタリング中は受け取りません。
  - `InputAudioDropOverlay.swift` は、音声ファイルまたは音声以外のファイルを中央の作業画面へドラッグしている間だけ出る案内表示を担当します。通常時の画面は変えません。
  - `VelouraSidebarView.swift` は、`音源` と `工程` の2セクションをつなぎます。
  - `VelouraMainWorkspaceView.swift` は、中央の作業画面と固定下部を担当します。横長波形、再生中スペクトル、ベクトルスコープ、横長スペクトログラム、直近ログ、全体工程をつなぎます。
  - `VelouraScrollIndicators.swift` は、各スクロール領域のバーだけを細いOverlayにし、スクロール中だけ濃さを抑えて表示します。ウインドウ内の別領域をまとめて変更せず、色も固定しません。「コントラストを上げる」が有効な時は標準の濃さへ戻します。
  - `AudioWaveformWorkspaceView.swift` は、A/B再生、音量、ラウドネス合わせ、入力・補正後・最終版の横長波形を担当します。波形は1つの`Canvas`で正負ピークとRMSを縦方向の区間として描き、中央線、再生済み範囲、共有カーソルを重ねます。クリック位置は音源全体の時刻へ変換して試聴機能へ渡し、ドラッグ量は表示範囲の移動だけに使います。波形の高さは各段で別に保持し、下端の`WaveformTrackResizeHandle.swift`から連続的に変更します。高さは画面内だけの表示状態で、音声データやアプリ設定には保存しません。
  - `WaveformTrackResizeHandle.swift` は、通常3段とStem 2段で共用する縦幅変更ハンドルです。初期96 pt、最小68 pt、最大192 ptに制限し、波形上の横ドラッグとは別の下端領域だけで上下ドラッグを受け取ります。ドラッグ量は、動くハンドル自身ではなく画面全体の固定座標から取得し、マウスの移動に連続して追従させます。
  - `WaveformTimeRulerView.swift` は、通常3段とStem 2段の波形で共用する表示範囲、時間軸右下の連続ズーム操作、波形データ数と表示幅から求める最大倍率、現在表示中の開始・終了時刻が分かる時間目盛り、手動の左右移動、再生位置を画面内へ保つ連続追従を担当します。これは表示専用で、音声処理や保存データは変更しません。
  - `AverageSpectrumComparisonView.swift` は、選択中のA/B比較に含まれる2音源について、共通再生位置のスペクトルを表示します。表示名は再生中スペクトルです。
  - `VectorScopeView.swift` は、再生中の1音源について左右チャンネルの関係を `Polar Sample`、`Polar Level`、`Lissajous` で表示します。標準のセグメント切り替え、クリップ点、相関メーター、L/Rバランスメーター、履歴リセットもここで扱います。停止中は枠と説明だけを表示します。
  - `LoudnessMeterView.swift` は、ベクトルスコープ右側のラウドネスメーターを担当します。Momentary LUFS、Short-Term LUFS、Integrated LUFS、True Peak dBTP、目標線、上限線を表示します。安全、注意、超過のような総合判定は出さず、上限超過は該当メーターの値と線の色で確認します。
  - `SpectrogramComparisonView.swift` は、入力、補正後、最終版のスペクトログラムを横長3段で表示します。
  - `WorkspaceFooterView.swift` は、下部の直近4件と全体工程を担当します。補正とマスタリングの実進捗率もここへ渡します。
  - `StemModeFooterView.swift` はStem側の直近4件と全体工程の内容を作り、配置寸法は`WorkspaceFooterLayout`を通常モードと共用します。
  - `RecentProcessingLogView.swift` は、日時、処理内容、対象ファイル、解析値、進捗を、直近4件まで表示します。
  - `OverallWorkflowView.swift` は、入力解析、補正処理、マスタリング、書き出しの現在位置と実進捗率を表示します。
  - `VelouraInspectorView.swift` は、開閉できる右設定パネルとして、右側の確認値、品質警告、完了後レポート、詳細設定を表示します。
  - `InspectorAnalysisPanel.swift` は、選択中音源のラウドネス、True Peak、ダイナミクス、ステレオ幅、品質警告を担当します。
  - `InspectorSettingsPanel.swift` は、右設定パネル専用の設定画面を担当します。補正、マスタリング、アプリ設定を切り替え、解析モード、アプリ全体背景の透明感、背景ぼかしの5段階調整はアプリ設定内に表示します。ぼかし調整はmacOS標準の`SliderTick`で5つの目盛りと段階名を表示し、`step: 1`で各目盛りの位置だけに止めます。OFFの時は5段階の選択UIを隠し、従来の透明感設定を使用していることを表示します。背景表示を調整しても、右設定パネルで選んでいる `補正 / マスタリング / アプリ` は維持します。補正の基本、掃除と修復、上級、マスタリングの基本、マスタリングの音色、マスタリングの上級はロータリーノブで表示し、それ以外の詳細設定は従来のスライダーで表示します。
  - `StemModeCorrectionControlList.swift` は、通常モードと同じ `DAWKnobControl` と列配置を使い、選択中のDrums、Bass、Other、Vocalsごとに12個の補正値を編集します。Stemごとの設定保持は分けたまま、基本は3列、掃除と修復は3列と2列、上級は2列ずつで表示します。
  - `StemModeMasteringSettingsView.swift` は、通常モードと同じプロファイルメニュー、説明、目標値、復帰操作、警告、基本・音色・上級の開閉カードと `DAWKnobControl` を使います。基本だけを最初から開き、補正またはマスタリングの実行中だけ編集を止めます。
  - `DAWKnobControl.swift` は、右設定パネルで使うロータリーノブの見た目と操作を担当します。固定画像、回転画像、数値、3点ラベル、項目名、`?` ヘルプ、透明な `− / ＋` 操作ボタンを重ねます。
  - `DAWKnobMetrics.swift` は、ロータリーノブの座標、回転角度、ドラッグ量、値の範囲制限をまとめます。値が少し変わった時にノブも少しだけ動くように、角度を連続して計算します。目標ラウドネス、ハーシュネス検出、Threshold のように値の範囲が広いノブは、上下ドラッグが重くならないよう専用のドラッグ倍率を持ちます。
  - `SettingHelp.swift` は、設定項目の `?` ヘルプに使うタイトル、読み方、説明文を保持します。
  - `TermHelpButton.swift` は、設定項目や比較項目の `?` 説明ポップアップを担当します。
  - `SidebarFileRow.swift` は、3音源のファイル名、保存場所、形式、サンプルレート、チャンネル、長さを担当します。
  - `SidebarProcessingStatusView.swift` と `SidebarProcessStatusRow.swift` は、補正とマスタリングの現在位置、経過時間、進捗率、全工程一覧を担当します。
  - `ProcessingLogView.swift` と `FullProcessingLogView.swift` は、補正ログとマスタリングログの全文を同じ幅の2列で表示します。詳細ログは、別ウィンドウや重ね表示ではなく、中央ワークスペースの通常表示領域を切り替えて表示します。
- `Sources/VelouraLucent/Models/`
  - 画面の状態、ノイズ除去の強さ、マスタリング設定、処理ルート判定です。
  - `AudioProcessingModels.swift` の `AudioFileInfo` は、実ファイルから取得した音声形式を保持します。
  - `AudioProcessingModels.swift` の `AudioMetricSnapshot` は、曲全体のステレオ相関と、時間ごとのステレオ相関推移を保持します。
  - `AudioProcessingModels.swift` の `VectorScopeSnapshot` は、ベクトルスコープの点、Polar Level の線、相関値、L/Rバランス値、入力チャンネルの状態を保持します。
  - `AudioProcessingModels.swift` の `LiveLoudnessMeterSnapshot` は、再生中の Momentary LUFS、Short-Term LUFS、Integrated LUFS、True Peak dBTP を保持します。
  - `AudioPreviewController.swift` は、試聴再生、A/B切り替え、再生中スペクトル、ベクトルスコープ、ラウドネスメーターの更新と消去を担当します。
  - `RecentActivityEvent.swift` は、固定下部に表示する直近の処理内容を、完全な文字列ログとは分けて保持します。
  - `AppAppearanceSettings.swift` は、アプリ全体の背景の透明感、背景ぼかし調整のON/OFF、5段階のぼかし設定と、通常、フルスクリーン、「透明度を下げる」に応じたウィンドウ表示状態を担当します。OFFはぼかしそのものを除去する意味ではなく、5段階調整を無効にして従来の`thin`へ戻す意味です。透明感と選択済みの段階は変更しません。スライダー操作中は表示値だけを動かし、設定保存は操作が終わった時に行います。「透明度を下げる」の切り替えだけでは保存値を変更しません。
  - `DisplayAnalysisStateStore.swift` は、入力、補正後、最終版ごとの表示解析状態を担当します。
  - `ProcessingProgressModels.swift` は、補正工程と進行イベントの名前、画面表示名、ログから進行状況を復元するための情報を担当します。
  - `ProcessingProgressStateStore.swift` は、補正とマスタリングの進捗状態を担当します。
  - `ProcessingSettingsState.swift` は、補正設定、マスタリング設定、解析モード、処理時に固定した設定を担当します。
  - `StemModelContract.swift` は、4ステムの役割、モデル入出力、正規化、AIモデルの取得・保存、receipt、同梱MLX実行資産、固定依存、manifestの読み取り形式を1か所にまとめます。
  - `StemModeSettings.swift` は、shifts、overlap、split、segment、batch size、seedを全て明示する分離設定です。音質とメモリへ影響するため、未承認のproduction既定値は持ちません。
  - `StemRoleAnalysisModels.swift` は、4役割それぞれのraw入力特徴を記録します。自動採否や候補比較の型は持ちません。
  - `StemCorrectionModels.swift` は、vocals / drums / bass / otherを必ず個別保持する補正設定、9つの補正工程の実行計画、DSP内部guard結果、1本の補正済みStemを記録します。
  - `StemWorkflowSession.swift` は、通常モードから独立した現在起動中のStem工程表示、ログ、成果物、検証、補正／マスタリングの直接キャンセル後状態をメモリで保持します。表示進捗は実処理の可否を決めず、処理成功時に完了表示へ確定します。
- `Sources/VelouraLucent/Services/`
  - 補正処理、倍音解析、foldover補完量推定、ノイズ除去、マスタリング処理、工程別診断WAVの書き出し、音量系の共通測定、比較用メトリクス計算、時間ごとの音量、平均スペクトル、曲全体と時間ごとの左右相関の計算、音声ファイルの読み書き、ファイル選択、完了通知などです。試聴中のスペクトルは `AudioPreviewController.swift` が `AVAudioEngine` の再生バッファから作ります。
  - `NativeAudioProcessor.swift` は、補正パイプライン全体の順番を担当します。`NativeAudioProcessorStages.swift` は、読み込み、解析、ノイズ除去、高域補完、各ガード、保存などの補正工程を分けて置いています。
  - `AudioSignalSampleRateConverter.swift` は、Stem専用のメモリ上`AudioSignal`を任意のサンプルレートへ変換します。変換前後のチャンネル、フレーム、NaN、Infinityを検証し、補正workflowがraw Stemを1回だけ48kHz処理信号へ変換する時に使います。通常モードの`AudioFileService`は変更していません。
  - `ConcurrentChannelProcessing.swift` は、左右チャンネルを同時に処理する共通処理を担当します。`NoiseMeasurementRunCache.swift` は、同じ音声のノイズ測定結果を再利用します。
  - `AudioAnalysisMode.swift`、`AudioAnalyzer.swift`、`HumRemover.swift`、`RumbleReducer.swift`、`SpectralGateDenoiser.swift`、`DenoiseMaskCoefficients.swift`、`DenoiseShimmerStabilizer.swift` は、補正パイプライン内で使う解析、ハム除去、低域ノイズ除去、ノイズ除去本体、ノイズ除去係数、シマー安定化を分けて置いています。
  - `SibilanceShimmerGuard.swift`、`CorrectionSibilanceBalanceGuard.swift`、`ShimmerPeakLimiter.swift`、`LowMidResidueGuard.swift`、`CorrectionHarmonicRepair.swift`、`CorrectionHighFloorPreserver.swift`、`CorrectionMudGuard.swift`、`LowBandPhaseSafetyGuard.swift`、`PeakSafetyLimiter.swift` は、補正パイプライン内のサ行保護、サ行の相対悪化防止、シマー制限、低中域整理、高域補完、補正後高域保持、補正後こもり確認、通常モードの低域位相確認、最終ピーク保護を分けて置いています。
  - `MasteringSignalMath.swift` は、マスタリング内で使う音量変更、帯域測定、帯域増減、ピーク上限、RMS、パーセンタイルなどの共通計算を担当します。
  - `MasteringToneStage.swift`、`MasteringDeEsserStage.swift`、`MasteringDynamicsStage.swift`、`MasteringSaturationStage.swift`、`MasteringAirEnhancer.swift`、`MasteringStereoStage.swift` は、マスタリング内の音色、ディエッサー、圧縮、倍音、空気感、ステレオ幅を分けて置いています。
  - `MasteringHighReturnGuard.swift`、`MasteringNoiseReturnGuard.swift`、`MasteringLoudnessStage.swift`、`MasteringFinalLowMidSafety.swift`、`MasteringFinalLoudnessStage.swift` は、高域戻り防止、ノイズ戻り防止、基本音量調整、最終低中域の安全確認、最終音量復帰と音量上限を分けて置いています。
  - `MasteringHighFloorPreserver.swift` は、マスタリング内で音楽成分として残す高域を守る処理と、その高域保持後のノイズ戻り確認を担当します。
  - `MasteringNoiseReturnSupport.swift` は、マスタリング内のノイズ戻り判定で使う軽量測定、基準高域測定、高域を削りすぎない候補確認を担当します。
  - `MasteringFinalNoiseReturnCeiling.swift` は、マスタリング終盤の最終ノイズ上限確認を担当します。
  - `DisplayAnalysisSupport.swift` は、画面に出す解析の時間計測と、表示解析を別タスクで動かす補助を担当します。
  - `StemModelAssetValidator.swift` は、manifest、revision、容量、SHA-256、config契約、receipt、同梱`default.metallib`を確認します。AIモデル欠落や不一致は復旧可能な状態として返し、通常モードを停止しません。同梱Metalの欠落はStem推論を止め、アプリ再インストールを案内します。
  - `StemModelAcquisitionService.swift` は、利用者確認後の2資産取得と原子的有効化、`StemModelInstallationStore.swift` は検証済み世代とactive pointer、`StemModelManager.swift` は復旧操作とUI状態を担当します。
  - `StemInputConversionService.swift` は、入力のchannel layoutを保持したままStem分離用44.1kHz / stereo / Float32をストリーミングで作り、再読込・容量・SHA-256検証後にだけ確定します。
  - `StemTemporaryAudioStore.swift` は、Float32 WAVを`.partial.wav`へ書き、sample rate、channel数、frame数、NaN／Infinityを検証してから最終名へ移動します。書込み失敗時は作成中の未完成ファイルだけを削除します。
  - `StemSeparationService.swift` は、モデル管理で確認済みのinstallationとmodel identityを明示入力として受け取り、再検証せずstrict local-only Demucsへ渡します。4 raw Stemは4本全ての書込みと音声検証が成功した時だけ有効とし、失敗時は今回の試行で新しく作ったraw Stemだけを削除します。開始前から存在したファイルは削除しません。
  - `StemAudioEvaluationService.swift` は、48kHz評価信号を通常モード共通の`AudioComparisonService`、`NoiseMeasurementService`、`AudioAnalyzer`、`MasteringAnalysisService`へ渡します。入力や再ミックスの評価境界では必要な時だけ48kHz化し、補正workflowから既に48kHzで渡された信号は再変換しません。`AudioAnalyzer`にはrunへ保存した解析モードを渡します。
- `StemRoleAnalysisService.swift` は、補正workflowが1回だけ48kHz化した処理信号を直接受け取り、集約表示用のmedian／IQRとは別に、同じ時間位置を保つ役割別保護profileを作ります。vocalsは非周期高域・子音活動・サ行帯域・声道スペクトル包絡・倍音列・声の芯、drumsは複素スペクトル変化・attack・帯域別decay、bassは時間追跡する基音・倍音列・50/60 Hz付近の音程整合・低域位相、otherは時間／帯域別decay・ambience・mid/side・広帯域位相を解析します。44.1kHz信号は受理しません。
- `StemRoleProtectionGuardService.swift` は、今回のraw Stem、各DSP直前Stem、DSP直後Stemの同一時間位置だけを比較します。サンプル由来の閾値や固定mappingを持たず、保護成分の存在量と当該DSP差分から連続的な弱化量を作ります。判断不能時は音を変えずDSP直前Stemを維持します。
- `StemCorrectionService.swift` は、raw解析から通常モード既存routeを決め、通常モードの下位DSPを同じ順序で使います。各DSP内部guardとStem役割別guardが返した1本だけを次工程へ渡し、DSP単位の弱化・スキップ・直前音維持を記録します。Stem個別limiterやStem専用完成候補評価を使いません。
  - `StemTransientRecoveryService.swift` は、ドラムのraw／補正済みStemを同じ時間位置で比較し、補正で不足した1.2〜12 kHzのアタック成分だけをrawに実在する向きとsample包絡線の範囲内で戻します。補正済み側が同等以上なら何も生成しません。
  - `StemMixService.swift` は、4StemのFloat32純粋加算とpeak測定だけを行います。No Vocals、自動gain preview、pan、width、tone、dynamics、limiterは作りません。
- `StemRemixService.swift` は、各Stemのactive program level、raw／補正済みStemの同じ20 ms活動区間で方向が揃う左右中心差、余韻・side成分の減少、drums／bassとvocals／otherの時間・帯域重なりから自動値を作ります。左右差が画面の中央表示境界`0.005`未満、方向不一致、有効区間不足の場合は自動panを`0`にします。再ミックス時はStem別gain、pan前の衝突区間だけの帯域ducking、Stem別pan、pan後send、減衰制御・拡散付き共有reverbの順で処理し、normalization、saturation、limitingは行いません。
- `StemValidationService.swift` は、分離、内部診断raw再ミックス、補正済み純粋加算、再ミックスの形式、長さ、有限値、残差、peak、相関、帯域差分、noise差分を記録します。構造的継続可否と解析issueを分け、解析値から音楽的合否や候補選択を作りません。
- `StemWorkflowService.swift` は、「補正を実行」「再ミックスを実行」「マスタリングを実行」を別APIでつなぎます。`StemRemixSafetyGuardService.swift`はrawに対する全体極性反転が明確なroleだけをrawへ戻します。純粋加算は比較基準として保存し、再ミックスは別成果物として保存し、マスタリングは検証済み再ミックスだけを既存処理へ渡します。
  - `StemWorkflowLogging.swift` は、現在セッションID、時刻、level、工程を持つStem専用ログを表します。保存履歴用の「最近の操作」状態は持ちません。
  - `StemWorkflowService.swift` は、現在セッションのWAVをmacOSの一時領域`VelouraLucentStemPreview/<session-id>`へ置きます。アプリ終了、入力変更、補正キャンセル時に該当セッションを削除します。AIモデルのApplication Support保存先とは別です。
- `Sources/VelouraLucent/Support/`
  - FFT まわりなどの共通処理です。
  - `InputAudioDropSupport.swift` は、ドロップされたファイルが実在する音声ファイルかどうかを確認します。音声以外のファイルやフォルダは入力として扱いません。
  - `AppResourceBundle.swift` は、開発中のSwift Packageリソースと、配布アプリの `Contents/Resources` を分けて探します。配布アプリで正式なresource bundleが欠けている時は、開発機の `.build` へ戻りません。
- `script/`
  - `prepare_stem_models.sh` は、開発・実モデル検証用にAIモデル2資産を固定revisionから準備する経路と、固定runtime・`mlx.metallib`・第三者noticeだけを準備する`--prepare-packaging`を分けます。配布準備modeはAIモデルcacheを読まず、取得・変更もしません。`--verify-packaging`もAIモデルの有無をRelease buildの条件にしません。vendored Demucsはレビュー済み51ファイルのパスとSHA-256がinventoryと一致する場合だけ使います。
  - `build_and_run.sh` は、AIモデル2資産を配布アプリへ入れず、`default.metallib`、manifest、第三者noticeを検証してReleaseビルドします。Metalライブラリが1個だけであることを確認し、内側のresource bundleからアプリ本体の順で署名・厳格検証した後だけ公開先を置き換えます。途中で失敗した時は公開中のアプリを残します。
- `Tests/VelouraLucentTests/`
  - 動作確認用のテストです。
- `Tests/Fixtures/AudioQuality/`
  - 通常の `swift test` で使う短い固定テスト音源です。煌びやかさ、空気感、ヒス、短時間シマー、総合マスタリング確認を、毎回同じ条件で確認します。
- `Docs/`
  - Metal / GPU 化のような、大きめの設計判断メモを置きます。
- `Resources/`
  - アプリアイコンの元画像です。`AppIcon-1024.png` を置きます。
- `Sources/VelouraLucent/Resources/`
  - Xcode から直接起動した時にも使える、実行時用の画像リソースです。
- `script/build_and_run.sh`
  - Releaseビルドでアプリを起動するスクリプトです。アイコン画像から macOS 用の Asset Catalog を組み立てて、`Assets.car` とアイコン情報を作ります。

## その技術を選んだ理由
- `SwiftUI`
  - Mac アプリの見た目を素直に組みやすいからです。
- `AVFoundation`
  - macOS 標準の音声読み書きが使えるからです。
- `UserNotifications`
  - macOS 標準の通知バナーと通知音を使えるからです。
- `Accelerate`
  - 周波数処理を Swift のまま実装しやすいからです。
- 段階式の自動処理
  - 非エンジニアでも、少ない選択で仕上がりを変えやすいからです。

## よくあるバグと修正方法
- 音声ファイルを開けない
  - 壊れたファイルか、対応外の形式の可能性があります。
- 出力ファイルができない
  - 元ファイルの場所に書き込み権限があるか確認します。
- MP3やM4Aにビット深度が表示されない
  - 圧縮音声にはWAVと同じ意味のビット深度がないためです。形式名、サンプルレート、チャンネル、長さを確認します。
- 左サイドバーで長いファイル名や保存場所が途中までしか見えない
  - 狭い幅で画面全体を押し広げないため、中央を省略しています。マウスを重ねると完全な内容を確認できます。
- 詳細ログの片側が見切れる
  - 補正ログとマスタリングログを同じ幅の2列にし、長い行を横へ伸ばさず折り返しているか確認します。
- `mp3` や `flac` 入力で書き出しに失敗する
  - 内部出力は `wav` に固定しています。利用者が保存するファイルは、選んだ用途に応じて `wav` または `m4a` になります。保存名の拡張子が選んだ用途と合っているか確認します。
- マスタリングが押せない
  - 先に補正を実行して、`*_lifter` を作ります。
- ノイズ除去を強くしすぎると音が細く聞こえる
  - まず `標準` で試して、必要な時だけ `強い` に上げます。
- 高域補完が強く聞こえる
  - 入力によっては倍音が少ないため、補完の効き方が変わります。
- 高域のザラつきが気になる
  - ノイズ除去の強さを上げると、高域の粒子感をより抑えやすいです。
- 高域が痛い
  - 詳細設定の `ハーシュネス抑制` を少し上げます。
- マスタリング後にシュワシュワ感が戻る
  - 通常は高域戻りガードを強くかけません。気になる場合は `エアー帯域` や `ハーシュネス抑制` を見直します。
- マスタリング後に音量が小さい
  - 最後に音量を再確認し、プリセットごとの音量変更上限、True Peak、ノイズ戻りを守れる範囲だけ戻します。ピーク余裕が足りない時は、無理に上げません。
- マスタリング後に煌びやかさや空気感が落ちる
  - 高域保持が、軽い 8kHz〜16kHz の帯域測定で確認します。ノイズが戻りすぎない範囲で最終版の高域を残します。
- 補正後に煌びやかさや空気感が落ちる
  - 補正後高域保持が、8kHz〜16kHz の実レベル低下を見て少し戻します。ヒス、シマー、サ行、16kHz以上の超高域が戻りすぎる候補は選びません。
- マスタリング後にサ行だけ強い
  - 補正後にサ行が強く残っている時は、最後に 5kHz〜9kHz の短時間ピークだけを軽く抑え、原音からの増えすぎを防ぎます。
  - 高域全体を下げるのではなく、短時間だけ目立つサ行ピークを対象にします。
- マスタリング後に高域が暗くなる
  - ノイズ戻りガードが、静かなヒスと短時間の高域チラつきを分けて確認します。8kHz〜20kHz が一度に落ちすぎる候補は選びません。
- 前のマスタリング結果が残って見える
  - マスタリング開始時に古い最終版プレビューを消し、新しい結果ができるまで書き出し対象にしません。
- 品質チェックで補正後の音量低下が出る
  - 補正後は音量を作る工程ではないため、まず情報として見ます。最終版でも低い時だけ注意として見ます。
- 品質チェックで高域低下やこもりが出る
  - `8kHz〜12kHz` の煌びやかさ、`12kHz〜16kHz` の空気感、`300Hz〜1kHz` のこもりを、入力、補正後、最終版の実測差分で見ています。
- ノイズチェックの表示が厳しく見える
  - `目立つ`、`少し目立つ`、`目立つ問題なし` で見ます。横棒の長さで、入力、補正後、最終版のどこで減ったか、どこで戻ったかを確認します。
  - 全体判定は、入力の悪さではなく、補正後や最終版に残っているノイズを基準にします。
  - `聴いて気になる場合の調整候補` は、実際に動かせる設定だけを出します。現在値とおすすめ値が同じになる時は表示しません。
  - 数値上の追加候補がない時も、最終版を聴いて違和感がないか確認する案内を出します。
  - ハムは低音全体ではなく、50/60Hzと倍音だけを周辺の周波数と比べます。低音楽器をそのまま電源ノイズ扱いしないためです。
- マスタリング後に広がるのに芯が弱い
  - `ステレオ幅` を下げます。いまは低域を広げない設計ですが、中高域は広がります。
- グラフが分かりにくい
  - まず上段で全体像を見て、次に再生中スペクトル、スペクトログラム、短期ラウドネス、ダイナミクス推移、相関メーターを確認すると分かりやすいです。
- 処理が重い
  - 長い音声や高負荷の環境では時間がかかります。同じ音声を共有できる場所では、読み込みとFFTを重ねないようにします。
  - ルートログの `実行工程数` と `スキップ工程数` を見ると、軽い素材でどの工程が減ったか分かります。
- 完了通知が出ない
  - アプリの「完了通知」設定、またはmacOSの通知許可が切れている可能性があります。必要に応じてONにします。
- どの工程で音が痩せたか分からない
  - 診断用の出力先を渡して、補正とマスタリングの中間WAVを工程ごとに書き出します。
  - 数値だけで判断せず、入力、補正途中、補正後、マスタリング途中、最終版を同じ音量感で聴き比べます。
- テストでノイズ値だけ良く見える
  - `Tests/Fixtures/AudioQuality/` の固定テスト音源を使い、ノイズ低下だけでなく、8kHz〜12kHz の煌びやかさ、12kHz〜16kHz の空気感、16kHz〜20kHz の超高域が残ることも確認します。
- Stem Modeでモデルが見つからない、または壊れている
  - アプリ全体のエラーにはせず、Stem Mode画面も置き換えません。右サイド「Stem分離」でモデル状態を確認し、必要な取得、修復、モデル検証、モデル再取得を行います。入力選択と入力表示解析は使えますが、補正はモデル利用可能まで押せません。通常モードへの切り替えは共通のモード切り替えから利用者が選びます。
- Stem Modeのモデル取得に失敗した
  - 失敗したstagingだけを片づけ、以前の検証済みactive世代を残します。一方のファイルだけを有効化しません。
- Stem Modeで`default.metallib`が見つからない
  - AIモデルの再取得では直りません。Stem推論を開始せず、通常モードとアプリ再インストールを案内します。
- multichannel音源をStem Modeへ入れた時に左右が分からない
  - チャンネル数だけで決めません。channel layoutが明確ならCore Audioが返した全係数を使います。unknown／discreteは1ch・2chでも自動判定せず、現在のlayout identityと一致する入力channel行×stereo出力列の正確な係数matrixを利用者に確認してもらいます。
- Stem再ミックスのピークが±1を超える
  - 純粋加算と再ミックスは有限なFloat32を保持し、固定-3 dBや再ミックス段のlimiterで隠しません。再ミックスの自動gainはraw／補正済みStemのactive program level差に限り±6 dB内で適用します。最終ピーク処理は既存Masteringへ渡し、A/Bのラウドネス合わせは再生時だけに使います。
- corrected stemで息、打点、低音の芯、残響が痩せる
  - 開発検証では、基本表示の独立Stem試聴欄で選択中役割のraw／補正後Stemを同じ時間位置と適切な音量条件で聴き、DSP内部guardが処理前信号を維持すべき箇所を確認します。個別Stemは入力2mix／純粋加算／再ミックス／最終版の2mix試聴欄へ混在させません。4Stemを純粋加算した後は、入力2mix、内部診断raw再ミックス、補正済み純粋加算、実行済み再ミックスの位相、相関、帯域、トランジェント、ステレオ感を解析しますが、数値だけで音質を断定したり自動棄却したりしません。
- Stem Modeのコードテストは通るが、実音源で同等以上かまだ分からない
- コード上は、補正済み4Stemと純粋加算の確定、自動／手動再ミックス、純粋加算A／再ミックスB、検証済み再ミックスだけを既存Masteringへ渡す三段階処理まで実装済みです。再ミックス設定変更、失敗、キャンセル時は補正基準を保持します。状態はメモリ、音声はsystem temporary directoryのWAVで保持し、永続run、checkpoint、設定／解析JSON、起動後再開はありません。2026-07-28に0.5秒入力と固定HTDemucs実モデルで補正、再ミックス、マスタリングの三段階を48.674秒で完走しました。実画面のStem Mode操作と完成音A/Bは未確認です。
- 2026-07-19の直接キャンセル・一時保存修正時点では、全728 tests / 79 suitesが1864.605秒で失敗0・終了時signal 11なしでした。その後の2026-07-22最終差分では、変更箇所へ直接対応する28 tests / 8 suitesと関連する99 tests / 14 suitesが失敗0、Debug／Release buildが成功しています。署名済み配布アプリの起動継続と通常モードの基本画面もAccessibilityで確認しました。実音・性能系を含む長大な全テスト直列実行は完了前に停止したため、2026-07-22差分の全テスト成功とは扱いません。Stem Mode実画面の全操作と完成音A/Bは未確認です。
- 2026-07-19、通常モードの既存補正・マスタリング経路にも192.999979秒の所有実音源を通し、Release構成で83.646秒、入力・補正済み・マスタリング済みの3ファイルを同じ長さ、48 kHz stereo Float32 WAVとして生成しました。これは処理経路と一時成果物の回帰確認であり、聴感上の音質不変やユーザー操作による書き出し完了を意味しません。
- 所有実音源runの既存構造検証は失敗0件、4Stemのraw fallbackは0件でした。通常モード共通reportでは、最終版が入力より2.7 LU低いことを`caution`、絶対値が既存境界を超えるハムを`warning`と表示しました。ハム自体は入力比2.3 dB低減しマスタリングでも戻っていないため追加操作は提案せず、高域チラつきの戻り1.9 dBに対する設定助言を1件だけ表示しました。これらは失敗や自動再処理ではなく、完成音を聴く時の確認情報です。
- アプリ再起動後にStem作業を再開できない
  - これは通常モードと同じ現行仕様です。Stem音楽処理の作業状態はメモリ、音声は一時領域だけに保持し、アプリ終了時に削除します。保存作業一覧、読込、再開はありません。

## 落とし穴と回避方法
- 圧縮音声へWAVのビット深度を当てはめない
  - MP3やAACは圧縮方式の名前を表示します。取得できない値を推測して画面へ出しません。
- 直近4件と完全な処理ログは役割が違います
  - 固定下部は今の状況を短く確認する場所です。全工程の記録は詳細ログに残します。
  - Stem Modeは入力、補正、再ミックス、マスタリング、利用者の書き出しを操作単位の直近項目として表示します。各DSPの内部工程は直近項目へ増やしません。
  - Stem Modeの詳細ログは、補正、再ミックス、マスタリングの人向け処理ログを3列で表示します。時刻、画面用工程名、成果物の`valid`状態、検証表示状態は混ぜません。
- 狭いサイドバーで工程名を長く説明しすぎない
  - 左サイドバーには全工程を出します。ただし各行は短い工程名にし、長い説明や完全なログは詳細ログで確認します。
- 処理は完全な AI モデルではありません
  - 高域補完は foldover を含む簡潔なヒューリスティック処理が中心で、補完量だけを軽い推定器で調整します。
- AIモデルの時限付き転送先URLを保存しない
  - 保存するのは40桁revisionを含むstable URLです。初回、修復、完全再取得は毎回stable URLから始め、`X-Repo-Commit`を確認します。
- AIモデルとMLX実行資産を同じ配布方法にしない
  - 重みとconfigは確認後にApplication Supportへ取得します。`mlx.metallib`は署名アプリへ同梱し、AIモデル再取得や外部Releaseの対象にしません。
- Stem入力に既存48kHz読込をそのまま使わない
  - 分離前にchannel layoutを確定する必要があります。専用変換でlayoutを記録してから44.1kHz / stereo / Float32を作ります。
- 50/60Hzが強いだけでbass stemをhum扱いしない
  - 音程、倍音、時間変化、低域位相を合わせて見ます。音楽の基音を電源ノイズとして削りません。
- Stem previewを左右別gainで安全化しない
  - ステレオ像が変わるため、全チャンネル共通gainだけを使います。raw再ミックスは変更しません。
- Stemごとにlimiterをかけない
  - 4本へ別々にlimiterをかけると、Stem同士の相対音量が変わり、純粋加算後のbalanceが変わります。Stemではpeakを記録し、最終制御は再ミックス後の既存Masteringへ任せます。
- Stemキャンセルで削除範囲を混同しない
  - 補正キャンセルは今回のStem一時WAVを削除し、選択入力、入力表示解析、設定を維持します。マスタリングキャンセルは後段の未完成WAVだけを削除し、補正済み4Stemと補正完了表示を維持します。
- モデル欠落時にHubや通常モードへ黙ってfallbackしない
  - Demucsは検証済みローカルモデル専用です。通常モードへ切り替えるかどうかも利用者が決めます。
- 補正は解析結果に応じて変わります
  - 倍音量、シマー、立ち上がり量、ノイズ量で効き方が少し変わります。
- マスタリングは別工程です
  - 補正と仕上げを分けて考えるほうが確認しやすいです。
- マスタリングの数値はアプリ内で同じ基準にそろえています
  - LUFS、True Peak、LRA は同じ測定サービスで出します。厳密な配信納品では外部メーターでの最終確認もあると安心です。
- Crest と LRA が下がりすぎると平坦になります
  - 比較表で `Crest` と `LRA` を見て、補正後から最終版で落ちすぎていないか確認します。
- 短期ラウドネスの線が横一直線に近いと、抑揚が減っている可能性があります
  - 最終版の線だけでなく、補正後との差を見ます。
- 再生中スペクトルは、選択中の2音源を同じ再生位置で比較します
  - 曲全体の平均ではなく、再生中の場所で周波数ごとの強さを見ます。
  - 入力、補正後、最終版をA/Bで切り替え、特に 8kHz から 12kHz あたりを聞き比べます。
- ノイズチェックは再生中スペクトルだけでは判断しません
  - 入力、補正後、最終版を、ヒス床、サ行瞬間差、シマー床、低域床、部屋床などの専用の測定値で見ます。
  - ノイズ床は、曲全体の平均音量ではなく、静かな区間を中心に見ます。音楽成分をノイズと間違えにくくするためです。
  - 横棒は変化を見やすくする補助です。正確な判断は、右側の実測値、差分、判定文で行います。
- サ行と高域のチラつきは別に見ます
  - `サ行・歯擦音` は発音の刺さり、`高域のチラつき` は高域の細かい揺れとして分けます。
- 改善アドバイスは、聴いて気になる場合に触る設定だけを出します
  - 補正で十分下がった時は補正設定を勧めません。マスタリングで戻った時は、マスタリング側の設定を優先します。
- ヒスと低域ゴロゴロは、静かな区間の床で見ます
  - ヒスは8kHz以上、低域ゴロゴロは20Hz〜150Hzの静かな区間を見て、補正の効き方を確認します。
- シマー制限のログで `軽量測定` が出た時は、長い音源の代表区間を使って測定しています
  - 強い補正では最後に全体確認も行い、速さだけで高域ノイズを残さないようにします。
- ノイズ戻りガードのログで `軽量判定` が出た時は、重い全体測定ではなく軽い判定で反復しています
  - 最後に全体確認を行い、ヒス、サ行、シマーの戻りをまとめて確認します。
- マスタリング後に `高域保持` のログが出た時は、ノイズ対策で高域が落ちすぎたため、音質バランスを保つ範囲で少し戻しています。
- `高域保持` は完全に取り消さず、ノイズが強い時でも最低限だけ残します。
- 補正後に `補正後高域保持/8-12kHz` や `補正後高域保持/12-16kHz` のログが出た時は、補正工程で痩せた煌びやかさや空気感を少し戻しています。
- `最終音量復帰` のログが出た時は、ノイズ戻りガード後に下がった音量、または安全に使える音量の余裕を、プリセットごとの目安超過幅を超えない範囲で、プリセットごとの音量変更上限、ピーク、ノイズを確認しながら最大2.0dBまで戻しています。目安超過幅は、自然と安全AI配信が0.75dB、聴きやすく整えるが1.0dB、YouTube / Spotify向けが1.25dB、押し出し強めが1.5dB、リリース音圧重視が2.0dBです。
- `ノイズ戻り: 緊急サ行上限` のログが出た時は、補正後から残ったサ行の短時間ピークだけを最後に抑えています。高域全体を暗くする目的ではありません。
- シマー制限の1回あたり最大削り量は、弱め2dB、標準3dB、強め4dBです
  - 短時間だけ飛び出るシマーを抑えつつ、持続する8kHz〜16kHzの煌びやかさと16kHz以上の空気感を残すことをテストで確認します。
- 相関メーターが0より左へ寄ると、モノラル再生で音が痩せる可能性があります
  - ステレオ幅を上げた時は、最終版の相関値も確認します。
  - 時間ごとの相関推移では、0より下に落ちた時間帯を確認します。長く0未満にある場合は、ステレオ処理や広がりを見直す候補です。
  - 無音区間は相関値として計算せず、前後の推移線をつなぎません。モノラル音源では左右の広がりを比較できないため、推移グラフではなく説明文を表示します。
- ダイナミクス保持と仕上げの強さは逆方向に効きます
  - 開放感を残したい時は `ダイナミクス保持` を上げ、前に出したい時だけ `仕上げの強さ` を上げます。
- 刺さり対策は高域全体の明るさと別です
  - `エアー帯域` を上げすぎたら、必要に応じて `ハーシュネス抑制` も調整します。
- 入力ごとに効き方が変わります
  - いつも同じ結果にはなりません。
- 出力形式は入力そのままではありません
  - `mp3` や `flac` を読んでも、内部のプレビュー用ファイルは `wav` になります。利用者が保存する時は、書き出しメニューで用途を選びます。
- 想定最大長付近のファイルはメモリ使用量が増えます
  - 入力変換と成果物の再検証はstreamingですが、現行Demucs APIは分離時に入力全PCMと4出力をメモリへ保持します。実利用上の想定最大長は約5分です。304.8秒の所有音源はproduction workflow全体を完走し、3曲を1つのRelease test processで連続処理した際も最大常駐メモリ約3.85 GiB、swap 0でした。10分以上の人工的な連結音源は実利用想定外のため必須検証へ使用しません。
- 完了通知は処理完了時だけ送ります
  - 進捗更新のたびに通知すると作業の邪魔になるため、補正とマスタリングの最終完了だけを対象にします。
- キャンセルを押してもすぐ止まらない
  - 音声処理は、現在実行中の重い工程が終わった後、次の工程へ進む前に停止します。画面に `キャンセル中` と出ている間は、同じボタンを繰り返し押さずに待ちます。
- エラーの詳しい内容を確認したい
  - 画面のアラートには確認方法を表示し、処理側の詳しいエラーは直近ログまたは詳細ログへ残します。
- ルート判定は処理開始直後に1回だけ決めます
  - 途中で判定が変わると、ログと実処理がずれやすいためです。
- 迷う素材は安全側で実行します
  - 音質と音楽成分保護を最優先にし、速度だけを理由に必要な工程を飛ばしません。同じ解析の使い回しや不要工程の根拠ある省略で、音を変えずに処理時間も短くします。

## Graphifyによるコード地図

- Graphifyは、コードや資料の「どこからどこへつながるか」を調べるための補助ツールです。
- このプロジェクトでは、Swiftなどのコード構造、設計資料、画像、音声の文字起こしを `graphify-out/` にまとめています。
- `graphify-out/graph.html` はブラウザで見る地図、`GRAPH_REPORT.md` は全体報告、`graph.json` は検索用データです。
- `graphify-out/` はこのMacだけで使う生成物です。Gitのコミット対象にはしません。
- CodexでGraphifyを明示して使う時は `$graphify` と入力します。
- コードについて調べる時は、次のコマンドを使います。
  - `graphify query "質問"`：関係する範囲を広く探します。
  - `graphify path "出発点" "到着点"`：2つの機能を結ぶ経路を探します。
  - `graphify explain "名前"`：1つのクラスや関数の周辺を説明します。
- Graphifyの関係には確かさの区分があります。
  - `EXTRACTED`：コードや資料に直接書かれている関係です。
  - `INFERRED`：名前や構造から推定した関係です。現行コードで確認してから使います。
  - `AMBIGUOUS`：確定できない関係です。事実として扱いません。
- コミット後とブランチ切り替え後は、Gitフックがコード部分のグラフ更新をバックグラウンドで開始します。
- 資料、画像、音声を変えた時は自動更新の対象外なので、`$graphify . --update` で更新します。
- グラフが古いと感じた時は `graphify update .` を実行し、`graphify-out/GRAPH_REPORT.md` の更新内容を確認します。
- Codexの別worktreeでは、自動フックは現在のコードを守るために動きません。必要な場合はそのworktreeで手動更新します。

## ベストプラクティス
- サイドバーを変更した時は、未選択、処理中、完了の各状態を確認します。画面幅を変えても、左の音源と工程、中央の表示、右の設定が見切れないことを確認します。
- 固定下部を変更した時は、直近4件の長いファイル名、解析値、進捗バーが同時に収まるか確認します。
- 詳細ログを変更した時は、補正とマスタリングの両方に長い行を入れ、閉じる操作、折り返し、縦スクロールを確認します。
- まずは `./script/build_and_run.sh` で起動します。
- アイコンを変える時は `Resources/AppIcon-1024.png` を差し替えます。
- Xcode から直接起動した時の Dock アイコンも、起動時に同じ画像へ上書きします。
- `swift build` と `swift test` を通してから変更を重ねます。
- 音声処理は「補正後」と「最終版」を分けて聞いて確認します。
- 補正が遅い時は、まず工程別ベンチでどの工程が重いか確認します。
- 軽い素材で処理速度を見たい時は、ルートログの `実行工程数` と `スキップ工程数` を確認します。
- `ノイズ除去` は `標準` を基準にして、必要な時だけ変えます。
- マスタリング詳細設定は、まず `ハーシュネス抑制` と `中低域` から触ると分かりやすいです。
- 解析を増やす時は、まず既に読み込んだ `AudioSignal` を使えるか確認します。
- 周波数帯の集計を増やす時は、毎回 `filter` せず、先にビン範囲を計算して使い回します。
- 比較表示に必要な値だけなら、重い解析サービスを丸ごと呼ばず、既存のFFT結果や軽い計算を使います。
- 短期ラウドネスやLRAのような音量窓は、サンプル列を毎回合計し直さず、累積エネルギーから計算します。
- LUFS、True Peak、LRA を使う時は `LoudnessMeasurementService` を通します。画面、グラフ、品質チェックで値がずれないようにするためです。
- `SpectralDSP.stft` を触る時は、補正、解析、スペクトログラムの全部に影響するため、関連テストと実測を必ず確認します。
- `SpectralDSP.istft` を触る時も、補正結果そのものに影響するため、パイプラインテストと実測を必ず確認します。
- `Spectrogram` の中身を触る時は、フレーム番号と周波数番号の位置ズレが起きないように、A/B確認を必ず行います。
- スペクトログラムの色を変更する時は、入力、補正後、最終版で同じ `0〜-100表示dB` を使います。低域と高域で色の基準がずれないよう、周波数バケット幅に合わせて表示値を補正します。
- ベクトルスコープを変更する時は、音声処理中の受け取り処理から画面状態を直接変更しません。計算結果だけを別の処理列から渡し、画面更新はメイン処理で行います。これを守らないと、再生ボタンを押した時にアプリが終了する原因になります。
- ベクトルスコープの軌跡を消す条件を変更する時は、停止、自然終了、ファイル変更をまとめて確認します。一時停止では軌跡を保持するため、停止との動作を混同しないようにします。履歴リセットは、再生中の音源のベクトルスコープ履歴だけを消します。
- ノイズ除去のmask式を触る時は、式を変えず、前計算版と従来式が一致するテストを先に置きます。
- mask適用の最適化では、ノイズ除去の式と出力を変えず、参照の仕方だけを軽くします。
- 並列化する時は細かく分けすぎず、波形、チャンネル、FFTのような大きなまとまりにします。
- マスタリング解析の数値を増やす時は、`magnitudes()` で大きな配列を作る前に、既存のスペクトログラムを直接走査できるか確認します。
- 解析や表示で周波数の強さが必要な時は、全フレーム分をまとめて作らず、`fillMagnitudes` や `fillMagnitudeHistory` で必要な分だけ使います。
- `medianFilter` を触る時は、旧実装と完全一致するテストを先に通してから使います。
- チャンネルごとの補正を並列化する時は、結果の順序を保ち、左右が入れ替わらないようにします。
- 差分を見る時は、入力・補正後・最終版の3つを同じ場所で見比べると理解しやすいです。
  - 音源変化を見る時は、短期ラウドネスで音量感、再生中スペクトルとスペクトログラムで帯域の形、ダイナミクス推移で押し固まり、相関メーターと時間ごとの相関推移で位相の安全性を確認します。
- 完了後レポートは、右側の音声確認エリアにあるボタンから開きます
  - LUFS、True Peak、音量変化、ノイズ、高域保持を1か所にまとめます。
  - 新しい解析は増やさず、入力、補正後、最終版の既存解析結果だけを使います。
  - 数値だけで合格や不合格を決めず、最終判断は試聴で行います。
- ノイズチェックを増やす時は、帯域の平均音量ではなく、静かな区間のノイズ床、瞬間的な突出、50/60Hzの周辺比のように測定方法を先に決めます。
- ノイズチェックの横棒を変更する時は、グラフを消さず、値、改善量、戻り量、判定が同じ行で読める形を保ちます。
- 進行状況を変える時は、ログ文の文字列判定ではなく `ProcessingProgressEvent` を使います。ログは人が読む説明、イベントは画面を動かす合図として分けます。
- 完了通知を変える時は、`ProcessingJob` から直接OS機能を呼ばず、`CompletionNotificationReporting` を通します。テストでは記録用の差し替え先を使い、本物の通知を送らずに確認します。
- 補正とマスタリングのキャンセルを変える時は、ツールバーの表示だけではなく、保持しているTask、別スレッドの作業、主要工程の停止確認、途中出力の削除をまとめて確認します。
- Stem Modeのキャンセルを変える時は、補正時のStem一時WAV削除、入力・入力表示解析・設定維持、マスタリング時の下流未完成WAVだけの削除、補正済み4Stem・補正完了表示維持、完了確定後のキャンセル禁止、アプリ終了・入力変更時の一時セッション削除をまとめて確認します。
- Stemモデル取得を変える時は、承認前に通信しないこと、stable URLの40桁revision、`X-Repo-Commit`、個別・合計容量、SHA-256、config契約、receipt、原子的active切替、既存世代保持を一組でテストします。
- Stemの役割別補正を変える時は、raw／補正後の各Stemと、入力2mix／raw再ミックス／補正済み純粋加算／実行済み再ミックスを既存解析・NoiseMeasurement・DSP内部guard・Stem役割別guardで自動確認します。ユーザーへStem単位の専門的な合否判定は求めません。性質の異なる複数の所有AI生成音源で解析接続と異常の有無を確認し、1曲の値を全音源の固定基準にしません。独立Stem試聴欄はraw／補正後を手動確認する機能であり、品質候補の選択には使いません。完成音の確認を求める場合は、純粋加算／再ミックス／最終版を「自然」「違和感がある」「判断できない」で確認します。
- Stemの役割解析を変える時は、44.1kHz raw正本を変更していないこと、補正workflowが1回だけ作った48kHz処理信号を直接解析していること、集約用median／IQRと本番guard用の時間整列profileを混同していないこと、全保護対象が有限値で同じframe位置に揃うこと、サンプル音源由来の絶対gateを増やしていないことを確認します。
- Stem工程guardを変える時は、今回のraw Stemと各DSP直前／直後だけを基準にしていること、問題区間のDSP差分だけを弱められること、判断不能・DSP失敗では当該DSP直前入力を維持できること、前工程で安全だった音まで取り消さないことを確認します。
- 純粋加算または再ミックスの検証を変える時は、sample rate、channel数、frame数、有限値などの構造的継続条件と、位相、相関、残差、帯域、noise、peak、Stem間共有成分変化の解析issueを混ぜないことを確認します。相関値の大小だけで自動棄却せず、明確な全体極性反転だけを該当roleのraw fallback対象にします。候補生成、AI選択、4Stem全体のraw fallbackは行いません。
- Stemの配布を変える時は、配布準備modeがAIモデルcacheを読まず変更もしないこと、配布アプリにAIモデル2資産が無いこと、`default.metallib`が1個だけあること、vendored 51ファイルのinventory・SHA-256、ライセンス表記、署名、モデルなしでのStandard Mode起動をまとめて確認します。
- メニューバー操作を増やす時は、`FocusedValues` を通して現在のウィンドウへ命令を渡します。ツールバーとメニューへ別々の処理を書かず、同じ操作関数を呼びます。
- 同じ音源のノイズ測定値は使い回し、同じ解析を何度も呼ばないようにします。
- 補正のハム検出では、50/60Hzと倍音の強さを測る時に、サンプルごとに三角関数を呼ばず、前回の値から次の値を計算します。判定式と出力は変えず、同じ測定を軽くします。
- 補正後のマスタリング用解析がすでにある時は、マスタリング開始時に再解析せず使い回します。
- 解析を使い回した時は、ログの `解析: 既存結果を使用` で確認します。
- マスタリングのノイズ戻りガードは、ヒス、サ行、シマーを一括で見て、途中経過と測定回数をログに出します。
- マスタリングのノイズ戻りガードは、最大3回の軽量判定で補正を進め、必要な時だけ最後に全体確認を1回行います。
- マスタリングでは、補正後から5kHz以上が落ちすぎないよう、5-8kHz、8-12kHz、12-16kHz、16kHz以上を分けて高域保持します。
- マスタリングの高域保持では、変化しない補正後音源と元音源の4帯域測定値を最初に1回だけ計算し、高域保持と最終高域保持で使い回します。処理途中で変化する音は、その時点の音を測ります。
- 最終音量復帰後もヒス、サ行、高域チラつきが大きく戻った時だけ、緊急上限ガードで弱く抑え直します。補正後よりヒスや高域チラつきが2.5dBを超えて戻った場合は、補正後基準の上限を守る範囲まで抑え直します。補正後で高域が大きく失われ、原音参照で高域を戻す素材では、原音より同じ種類のノイズが増えない範囲で、音楽的な高域として原音参照を優先します。ヒスは復帰前も確認し、復帰前が上限内なら、高域を追加で削らず音量復帰前へ戻します。復帰前も上限を超える場合は、既存の緊急上限ガードを続けます。
- 内部判定監査表は `Docs/InternalAudioJudgementAudit.md` にまとめています。
- 補正の低域ノイズ整理とシマー制限も、測定回数をログで確認できます。
- 補正のノイズ除去ログは、解析済みのSTFT結果から高域メトリクスを使い回し、ログ用の再STFTを避けます。
- マスタリング解析の帯域集計は、Metal対応Macでは本処理でMetalを使い、STFT全体を保持せず分割して計算します。ログでCPU経路かMetal経路かを確認できます。
- 無理に複雑な仕組みにせず、今の処理順を保って改善します。
